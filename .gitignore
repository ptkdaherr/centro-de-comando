# Arquivo solto, não pertence ao projeto
Timed out questions defaults.zip

# Lixo de sistema
.thumbnail
Thumbs.db
.DS_Store

# Futuro empacotamento (Electron/Tauri/NW.js)
node_modules/
dist/
build/
target/
out/
*.log

# Backend (Fase 3a) — segredos e dados locais, nunca versionar
server/.env
server/data/

# Prints/capturas — podem ter dados pessoais (conta, billing). Nunca versionar.
screenshots/
ideias/
*.zip

# Anotações locais do deploy (tokens, códigos) — NUNCA versionar.
*.local.txt
DADOS-DEPLOY*

# Tauri (Fase 3d) — build, código gerado e o Node embutido (sidecar)
src-tauri/target/
src-tauri/gen/
src-tauri/binaries/
