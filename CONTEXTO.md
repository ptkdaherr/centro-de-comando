# Centro de Comando — versão .exe (contexto do projeto)

> ⚠️ **Manter este arquivo atualizado.** Toda vez que mudarmos algo relevante aqui (nova tela, decisão de arquitetura, escolha de framework pro .exe, etc.), atualizar a seção correspondente e registrar no changelog no final. Não deixar esse arquivo ficar desatualizado.

## O que é isso

Esta pasta é uma **segunda linha de desenvolvimento** do projeto "Centro de Comando" (ERP/PSA da agência do usuário). A pasta oficial é `C:\Users\patri\Desktop\SITE\CENTRO-DE-COMANDO` (HTML/CSS/JS vanilla, já com git e em produção incremental). Esta pasta (`Centro de comando - exe`) nasceu de uma versão mais avançada visualmente — `Centro de Comando.dc.html` — feita num framework single-file diferente, com a meta explícita de **virar um executável (.exe)** que abre no PC (e eventualmente no boot).

As duas pastas evoluem em paralelo por enquanto. Não foi decidido ainda se esta substitui a oficial ou se ficam como protótipos separados — tratar como decisão aberta.

## Stack / Framework: "DC"

`Centro de Comando.dc.html` não é HTML puro: é um arquivo único que usa um framework próprio chamado **DC** (Design Component), rodado via `support.js` (53 KB, runtime compilado de TypeScript).

Como funciona:
- Dentro do HTML existe um bloco `<x-dc>` com uma sintaxe XML customizada: `<sc-if>`, `<sc-for>` (controle de fluxo), `<dc-import>` (referência a componentes), `<sc-helmet>` (gerência de `<head>`).
- Interpolação estilo mustache: `{{ variavel }}`, `{{ caminho.aninhado }}`, comparações (`===`, `!==`).
- Handlers de evento (`onClick`, `onChange`, `onInput`...) compilados para eventos React.
- Estilo via `style="{{ objetoCss }}"` ou CSS inline com variáveis interpoladas; pseudo-classes (`style-hover`, `style-focus-within`) viram classes CSS geradas.
- Lógica de componente: classes que estendem `DCLogic` (alias de `StreamableLogic`), com `state`, `setState()`, `renderVals()` — modelo parecido com React clássico, sem Redux/MobX.
- **Sem build step.** O `support.js` faz todo o trabalho em runtime: parse do template, compilação para funções de render React, carregamento de React 18.3.1 / ReactDOM via CDN (unpkg, com SRI hash), hot-reload de componentes (`window.__dcUpdate`), registry de componentes (`window.__dcRegistry`).
- APIs globais expostas: `window.DCLogic`, `window.__dcBoot()`, `window.__dcUpdate(...)`, `window.getDC(nome)`.

Avaliar: é uma dependência de um framework não-padrão (não é Next/Vite/CRA). Isso facilita edição "sem build" mas dificulta portar pra ecossistema React convencional se algum dia quisermos.

## Telas já construídas (no `.dc.html`)

1. **Início** — KPIs (demandas ativas, taxa de entrega, a receber, urgentes), tarefas de hoje, próximas demandas, feed de atividade recente.
2. **Demandas** — Kanban por status (A fazer → Em produção → Em revisão → Entregue) ou por cliente (lanes coloridas). 12 demandas de exemplo.
3. **Pessoal** — Projetos próprios (vlog, portfólio, estudo de AE) + rascunhos de ideias.
4. **Calendário** — Toggle mês/semana, eventos com bolinhas coloridas por cliente.
5. **Clientes** — Cards de clientes ativos (nome, status, próximo vencimento, dia de pagamento), funil de prospecção (Prospecção → Proposta → Ativo → Pausa), lista de prospects.
6. **Financeiro** — KPIs (recebido, a receber, custos, líquido), gráfico de barras de 6 meses, lista de transações.
7. **Salário** — Calculadora de recomendação (58% do disponível), breakdown (recebido − custos − 20% reserva), histórico de 5 meses.
8. **Metas** — 4 cards de progresso (faturamento, entrega no prazo, clientes ativos, horas produtivas).
9. **Ideias** — 6 cards com tags/status/data, botão "transformar em projeto".
10. **Workflow** — Canvas tipo node-graph (7 nós, linhas SVG conectando ENTRADA → TAREFAS → REVISÃO → SAÍDA).
11. **Assistente (chat IA)** — Layout split: sidebar de contexto/sugestões + chat à direita, input com envio (ainda sem IA real conectada — é teaser/mock).

Design: tema escuro, fonte Geist (display + corpo) e Geist Mono, paleta neutra (bg/surface/text em variações) + cores semânticas (accent azul, ok verde, warn âmbar, crit vermelho, info ciano). Visual "macOS app" — bem mais polido que a versão vanilla da pasta oficial.

## Modelo de dados (tudo mockado, em memória)

```
Clients: { id, nome, initials, color, tipo (fixo/freela), valor, pagDia, servico, since }
Demands: { id, title, clientId, status, type, due, flag }
Prospects: { nome, segmento, obs, etapa }
PersonalProjects: { title, status, type, due }
Ideas: { title, status, tags, date }
WorkflowNodes: { left, top, type, title, statusLabel }
```

**Nenhuma persistência implementada ainda** — os dados são state em memória e resetam ao recarregar a página. Isso é a lacuna mais importante antes de qualquer release.

## O que falta implementar (atualizado em 2026-06-17 — pós Fase 1)

**Resolvido na Fase 1:**
- ✅ **Persistência via `localStorage`.** `_persistKeys`/`_storageKey` + `componentDidMount` (restaura) e `setState` sobrescrito (salva a cada mudança). Chave `cdc-exe-state-v1`. Cobre `clients`, `prospectos`, `demands`, `ideias`, `lancamentos`, `wfNodesExtra`.
- ✅ **Bug do drawer corrigido.** `submitDrawer` agora tem branches `lancamento` e `ideia` que de fato adicionam aos arrays `lancamentos`/`ideias` (antes só fechava o drawer e mostrava toast falso).
- ✅ **Ideias com formulário funcional.** Campos título/categoria/observações do drawer de ideia agora têm `value`/`onInput`/`onClick` ligados a state real (antes eram inputs soltos, sem handler nenhum).
- ✅ **Alerta de cliente atrasado.** `clientCards` agora calcula `isLate` com base em `cl.pagDia` (clientes fixos) vs. dia atual (`todayReal`) — status muda pra "Atrasado" com cor `var(--crit)`. Heurística simples (sem histórico de pagamento real ainda).
- ✅ Posição dos nós extras do Workflow: confirmado que já funcionava corretamente (não era bug).

**Resolvido na Fase 2:**
- ✅ **Editar/excluir demandas, clientes, prospectos, lançamentos e ideias.** Padrão `drawerEditId` (null = criar, id = editar) em `submitDrawer`. Cada entidade tem `editX`/`deleteX` (com `window.confirm` antes de apagar). Demandas: edição funciona tanto no kanban por status quanto nas lanes por cliente (cards "Pessoal" continuam não-editáveis via `mkLane(d, false)`).
- ✅ **Drag-and-drop real no kanban** (visão por status). `draggable="true"` + `onDragStart`/`onDragOver`/`onDrop` nativos (HTML5 DnD), atualiza `status` da demanda ao soltar numa coluna.
- ✅ **Busca ⌘K funcional.** Badge do topo agora abre modal (clique ou `Cmd/Ctrl+K`); pesquisa por título/nome em demandas, clientes e prospectos; clicar num resultado abre o drawer de edição já preenchido e navega pra tela certa. `Esc` ou clique fora fecha.

**Resolvido na Fase 3a:**
- ✅ **Backend local real.** `server/index.mjs` substitui o `tools/dev-server.mjs` como forma principal de rodar o app: serve os arquivos estáticos (com o mesmo live reload de sempre) **e** expõe API REST autenticada. `localStorage` deixou de ser fonte de verdade — agora é só cache visual durante a sessão; os dados reais vivem em SQLite no servidor.
- ✅ **Login obrigatório.** Tela de senha antes de qualquer dado carregar (`showLogin`/`authed` no state). Sem sessão válida, `/api/state` responde 401 e nada é exibido.
- ✅ **Banco SQLite** (`server/data/cdc.sqlite`, fora do git) com uma tabela (`state_store`) guardando cada entidade (`clients`, `prospectos`, `demands`, `ideias`, `lancamentos`, `wfNodesExtra`) como uma linha JSON — mesma semântica de "snapshot completo" que já existia no localStorage, só que persistente e compartilhada entre dispositivos.
- ✅ **Senha com hash `scrypt`** (`node:crypto`, zero deps), nunca texto puro. Configurada via `npm run setup` (escreve em `server/.env`, gitignored).
- ✅ **Sessão HTTP-only.** Cookie `cdc_session` (`HttpOnly; SameSite=Lax`), token opaco aleatório guardado em memória no servidor (não sobrevive a restart — reinício do processo força novo login, decisão deliberada pela "segurança máxima").
- ✅ **Rate limit de login.** 5 tentativas erradas por IP → bloqueio de 5 minutos.
- ✅ **`.gitignore`** ganhou `server/.env` e `server/data/*.sqlite` — confirmado via `git status --ignored` que nenhum dos dois aparece como rastreável.

**Resolvido na Fase 3b:**
- ✅ **Assistente conectado a uma IA de verdade (Gemini).** O chat (`Centro de Comando.dc.html`) agora envia o histórico pra `POST /api/chat`, que chama a API do Gemini com *function calling* — o modelo pode responder em texto (perguntas sobre os dados) ou propor uma ação (criar/editar/excluir).
- ✅ **Confirmação obrigatória pra toda ação** (criar, editar OU excluir — não só excluir). O backend nunca executa nada: só decide *o quê* fazer e devolve um `pendingAction` com um resumo em português. O frontend mostra um card com "Confirmar"/"Cancelar" no próprio chat; só depois de clicar em Confirmar a ação é de fato aplicada (`aiApplyAction`, reaproveitando os mesmos helpers de `submitDrawer`/`editX`/`deleteX` já existentes) e salva (mesmo `PUT /api/state` debounced da Fase 3a).
- ✅ **Log de auditoria** (tabela `audit_log` no SQLite): toda ação proposta pela IA é gravada com status `proposed` → `applied` ou `rejected`, com timestamp, nome da ferramenta, argumentos e resumo. Painel "Histórico de ações" na sidebar do Assistente lista isso (`GET /api/audit`).
- ✅ **Resumo das ações sempre identifica o registro pelo nome**, não só pelo id — inclusive em edições (ex: `Editar cliente "Luso Automóveis" → "Luso Automóveis Premium"`), pra quem for confirmar entender exatamente o que vai mudar antes de clicar.
- ✅ **Chave do Gemini nunca chega ao navegador** — fica só em `server/.env` (gitignored), o frontend nunca a vê; todas as chamadas à API do Gemini saem do servidor.
- ✅ **Rate limit do chat:** no máx. 50 mensagens/hora por IP, separado do rate limit de login — protege contra estourar a cota grátis da API por engano (loop, clique acidental repetido, etc.).
- ✅ **Sem chave configurada, o app não trava:** servidor sobe normal, e o chat só avisa (mensagem amigável) que falta configurar, ao tentar enviar uma mensagem.
- ✅ **`npm run setup` estendido** — agora também pergunta (de forma opcional, pode pular com Enter) a chave do Gemini, e preserva a chave existente se você rodar de novo só pra trocar a senha.
- **Desvios conscientes em relação ao plano original:** o plano citava o modelo `gemini-2.5-flash` e autenticação via `?key=` na URL; na implementação usei **`gemini-3.5-flash`** (mais novo, confirmado disponível no tier grátis, e o próprio plano já previa o modelo ser trocável via `GEMINI_MODEL` no `.env`) e autenticação via header `x-goog-api-key` (forma atual recomendada pela documentação oficial, em vez de colocar a chave na URL — mais seguro contra a chave aparecer em logs de acesso).

**Resolvido na Fase 3c:**
- ✅ **PWA instalável** (manifest + service worker + ícones gerados sem deps). App vira janela standalone (sem barra de URL) no desktop e ganha modo "tela cheia" no iOS via meta tags apple. Ver seção "Arquitetura PWA (Fase 3c)" abaixo — inclui a limitação de secure-context (SW/instalação Android exigem HTTPS; LAN por IP não basta).

**Bom ter / próximas sub-fases da Fase 3:**
- 3d — Empacotamento Tauri (.exe) consumindo o mesmo backend local.
- Heurística de cliente atrasado pode evoluir pra modelo de pagamento real (hoje é só dia-do-mês vs `pagDia`, sem registrar se aquele mês já foi pago).

## Arquitetura de backend (Fase 3a, 2026-06-17)

O app deixou de ser "só frontend com localStorage" e passou a ter um **backend local único** que serve tanto o navegador (PC ou celular, mesma rede) quanto, futuramente, o executável Tauri (Fase 3d) — todos conversando com a mesma fonte de dados.

```
server/
  index.mjs       servidor HTTP: estáticos + live reload + API REST
  db.mjs          SQLite (node:sqlite nativo) — schema + getState()/saveState()
  auth.mjs        hash/verify de senha (scrypt), sessão em cookie HTTP-only, rate limit
  setup.mjs       script de uso único ("npm run setup") — define a senha inicial
  .env            gitignored — guarda APP_PASSWORD_HASH (e, na Fase 3b, GEMINI_API_KEY)
  .env.example    comitado — só os nomes das chaves, sem valores
  data/
    cdc.sqlite    gitignored — banco real, fonte única de verdade dos dados
```

**Rotas da API:**
- `POST /api/login` — `{ password }` → seta cookie de sessão ou 401 (429 se rate-limited).
- `POST /api/logout` — limpa a sessão.
- `GET /api/me` — `{ ok: true|false }`, usado no boot do app pra decidir login vs. dashboard.
- `GET /api/state` — (autenticado) devolve o snapshot salvo (`clients`, `demands`, etc.).
- `PUT /api/state` — (autenticado) grava o snapshot enviado pelo frontend (debounced no cliente, 400ms).

**Decisão de simplificação consciente:** o plano original previa "uma tabela por entidade" no sentido relacional; na prática, como o frontend já trata cada entidade como um array que substitui por completo a cada mudança (nunca faz update parcial de um registro), implementei como uma única tabela `state_store` com uma linha JSON por entidade — mesmo comportamento do `localStorage` de antes, agora durável e compartilhado, sem inventar um mapeamento relacional que nada no app usa hoje. Fácil de evoluir pra tabelas relacionais de verdade se um dia precisar de queries cruzadas (ex: relatórios por cliente).

**Pendência conhecida e deliberada:** como a hospedagem por enquanto é só local/LAN (decisão "local primeiro, decide depois"), o cookie de sessão não tem a flag `Secure` (exigiria HTTPS). Isso é seguro no contexto atual (tráfego não sai da rede local) e fica documentado como algo a resolver junto da decisão de hospedagem pública.

## Arquitetura do Assistente IA (Fase 3b, 2026-06-17)

**Por que a execução da ação acontece no frontend, não no backend:** criar/editar/excluir já é lógica do `Component` em `Centro de Comando.dc.html` (`submitDrawer`, `editCliente`, `deleteDemanda` etc.), que sabe gerar `id`, cores de cliente, ícones de tipo de demanda etc. Em vez de duplicar essa lógica em Node, o backend só decide **o quê** fazer (via Gemini); o frontend, depois da confirmação do usuário, executa a ação reaproveitando essas mesmas funções.

```
Usuário digita no chat
  → frontend envia histórico pra POST /api/chat (autenticado, rate-limited)
  → backend monta contexto (estado atual + data de hoje) + chama Gemini com 15 "tools"
      (create/edit/delete para demanda, cliente, prospecto, lançamento, ideia)
  → Gemini responde com texto e/ou um pedido de function call
  → backend NÃO executa a ação; grava em audit_log (status 'proposed') e devolve
      { reply, pendingAction: { id, tool, args, summary } } pro frontend
  → frontend mostra a resposta da IA + (se houver) um card de ação com Confirmar/Cancelar
  → Confirmar → aiApplyAction(tool, args) → setState → persiste via PUT /api/state
      → POST /api/chat/resolve {id, status:'applied'}
  → Cancelar → POST /api/chat/resolve {id, status:'rejected'}, nada é salvo
```

```
server/
  tools.mjs       15 function declarations (create/edit/delete × 5 entidades) + summarizeAction()
  gemini.mjs      askGemini() — chama generativelanguage.googleapis.com via fetch nativo
  db.mjs          + tabela audit_log (id, created_at, tool, args, summary, status)
  index.mjs       + POST /api/chat, POST /api/chat/resolve, GET /api/audit, rate limit do chat
```

**Modelo:** `gemini-2.5-flash` (configurável via `GEMINI_MODEL` no `.env`, sem alterar código). Auth via header `x-goog-api-key` (não na URL). Sem SDK — só `fetch` nativo do Node, mantendo a filosofia zero-dependências da 3a.

**Histórico da escolha do modelo (2026-06-17):** inicialmente usei `gemini-3.5-flash` (mais novo, encontrado na documentação oficial). No teste em produção com a chave real do usuário, esse modelo retornou erro 503 ("sobrecarregado") de forma consistente — testei direto contra a API do Google com 4 nomes de modelo diferentes e só `gemini-2.5-flash` respondeu 200 de forma estável. Voltei o padrão pra `gemini-2.5-flash` (que é, aliás, o modelo do plano original) e confirmei com testes reais: resposta de texto simples, function calling resolvendo nome→id corretamente ("cliente Luso" → `clientId` certo), e o guard-rail contra invenção de id (perguntou "Padaria do Zé" inexistente → modelo pediu confirmação em vez de inventar um id). Se no futuro `gemini-3.5-flash` se estabilizar, é só trocar `GEMINI_MODEL` no `.env`, sem mexer em código.

**Anti-alucinação:** o `systemInstruction` envia o JSON completo do estado atual e instrui explicitamente "nunca invente um id que não apareça nos dados — se não tiver certeza, pergunte". Vale revisar na prática (pedir uma ação citando cliente/id inexistente) e ajustar esse texto se o modelo inventar algo.

## Arquitetura PWA (Fase 3c, 2026-06-19)

O app agora é um **PWA instalável**. Objetivo: abrir como app de verdade (janela standalone, ícone próprio, sem barra de URL) no desktop e dar uma experiência "de app" no celular — um passo antes do empacotamento Tauri (3d), reaproveitando o mesmo backend local.

```
manifest.webmanifest    nome, ícones, display:standalone, theme/background color
sw.js                   service worker (network-first + fallback de cache)
icons/
  icon.svg              logo vetorial (favicon + ícone "any" do manifest)
  icon-192.png          ícone 192 (Android/desktop)
  icon-512.png          ícone 512 (splash, lojas)
  icon-maskable-512.png ícone com safe zone (Android adaptive)
  apple-touch-icon.png  180×180 p/ iOS
tools/gen-icons.mjs     gera os PNGs acima a partir do logo (npm run icons)
```

**Ícones gerados sem dependência externa.** `tools/gen-icons.mjs` desenha o logo do app (quadrado com gradiente azul `--accent`→`--accent-2` + quadrado branco) e escreve PNGs reais usando só `node:zlib` (encoder PNG próprio: CRC32 + chunks IHDR/IDAT/IEND, antialias por supersampling 4×4) — mantém a filosofia zero-deps das fases 3a/3b. `npm run icons` regenera tudo; não roda no boot. O `icon.svg` é escrito à mão.

**Service worker — estratégia network-first.** Sempre tenta a rede primeiro (então o live reload do dev e os dados nunca ficam presos em cache) e só cai pro cache quando a rede falha; em navegação offline, serve o app shell (`/`). Na primeira carga online ele cacheia o shell + React/ReactDOM (unpkg) + as fontes (Google), então recargas seguintes ficam resilientes a blips de CDN. **Regra de ouro:** o SW NUNCA intercepta `/api/*` (precisa de cookie de sessão e resposta fresca) nem `/__livereload` (stream SSE keep-alive) — ambos passam direto pro browser. Nome do cache versionado (`cdc-pwa-v1`); o `activate` limpa versões antigas.

**Ligação no app:** tags no `<head>` estático de `Centro de Comando.dc.html` (`<link rel=manifest>`, `theme-color`, `apple-touch-icon`, `apple-mobile-web-app-*`, favicon SVG) + um registro de SW defensivo (`if ('serviceWorker' in navigator)`, no evento `load`). No servidor (`server/index.mjs`) só foi preciso registrar o MIME `.webmanifest` — os ícones, o `sw.js` e o manifest já são servidos pela rota de estáticos existente (a raiz, fora de `server/`).

**Limitação importante — secure context.** Service Worker e a instalação automática do Chrome/Android exigem **HTTPS ou localhost**. No desktop via `http://localhost:5174` funciona 100%. Mas no **celular via `http://192.168.1.2:5174` (IP de LAN sem TLS) o SW não registra** — o registro é defensivo e simplesmente não roda lá, sem erro no console. Mesmo assim, no **iOS** o "Adicionar à Tela de Início" já abre em modo standalone graças às meta tags `apple-mobile-web-app-*` (não depende do SW). Pro Android instalar como app e pro SW rodar no celular, precisa de HTTPS — o que chega "de graça" com o Tauri (3d, que serve via protocolo próprio = secure context) ou com a futura hospedagem com TLS. Ou seja: o PWA está completo e **pronto pra brilhar assim que houver HTTPS**, sem mais trabalho de frontend.

**Verificação (ao vivo, Playwright/Chromium headless contra o `npm run server` real, login `teste123`):** SW registra com escopo `/` e fica ativo; após um reload passa a **controlar** a navegação (`controller` ≠ null); o app renderiza e loga normalmente com o SW no caminho; `/api/me` responde `{ok:true}` fresco — **não interceptado nem cacheado**; o cache `cdc-pwa-v1` guarda os 11 itens do shell + React/ReactDOM + fontes e **nenhuma** rota `/api`/`__livereload`; manifest é JSON válido com os 4 ícones (any + maskable, 512 presente). **Zero erro de console/página.** Os 4 PNGs foram validados à parte (assinatura PNG, IHDR color-type 6/8-bit, IDAT descomprimindo no tamanho exato de scanlines). Script de teste e o `playwright` (instalado com `--no-save`, Chromium já em cache) removidos ao final — working tree limpo.

## Tema claro/escuro (2026-06-19)

O app agora tem **tema claro além do escuro**, alternável e persistente. O escuro continua o padrão.

Como funciona:
- Todas as cores são CSS variables. Os dois conjuntos vivem em `:root, html[data-theme="dark"]` e `html[data-theme="light"]`, num `<style>` no `<head>` **estático** (não no helmet) — assim valem já no boot, antes do React, evitando flash de tema errado. Um script no mesmo `<head>` lê `localStorage['cdc-theme']` e aplica `data-theme` ao `<html>` antes do paint.
- O toggle (botão ☾/☀ no titlebar) alterna `data-theme` no `<html>` + salva no `localStorage` + atualiza o `theme-color` do PWA. O state `mode` controla o ícone (sol no escuro, lua no claro) e o título do botão.
- O sistema de **accent** que já existia (`applyTheme()`, temas Azul/Verde/Roxo/Grafite) continua funcionando por cima: ele seta `--accent*` inline no root, sobrepondo o tema. Por isso o root mantém só o trio `--accent` inline; o resto das variáveis vem do `:root`.
- Além das ~25 variáveis de cor, o tema controla: fundo do app (`--app-bg`/body), o gradiente do root (`--app-gradient`), a scrollbar (`--scrollbar`/`--scrollbar-hover`) e os fios do canvas do Workflow (`--wire`) — tudo que antes era escuro hardcoded.

Quase tudo já era variável (1138 usos de `var(--*)`), então a paleta clara foi uma **redefinição de variáveis**, não uma reescrita. Persistência via `localStorage` (preferência local do dispositivo, não vai pro backend).

**Telas/elementos que eram hardcoded:**
- ✅ **Início** e **badges da sidebar** — resolvidos em 2026-06-19 (ver changelog): KPIs/listas da Início e os badges agora **derivam do `state`** (0/empty quando vazio, dados reais quando há). "Atividades recentes" fica em empty state fixo (ainda não há um log de atividades real no modelo de dados).
- ⏳ **Ainda hardcoded:** provavelmente partes de **Metas/Salário/Calendário** (cards/valores de exemplo) e a data fixa "Segunda · 15 de junho" no header da Início. Conectar conforme for necessário rumo ao app real.

## Arquitetura do executável (Fase 3d, 2026-06-20) — Tauri + Node embutido

O app virou um **.exe autônomo** via Tauri (núcleo Rust + WebView2 do Windows). O backend Node **não foi reescrito**: o .exe embute o `node.exe` e roda o mesmo `server/index.mjs` como sidecar.

```
src-tauri/
  Cargo.toml, build.rs       projeto Rust
  tauri.conf.json            janela criada via código, resources, ícones, bundle NSIS
  src/lib.rs                 inicia o Node sidecar, espera a porta 5174, abre a janela, mata o Node ao fechar
  src/main.rs                entrypoint
  capabilities/default.json  permissões da janela "main"
  ui/index.html              placeholder do frontendDist (a janela carrega http://localhost:5174)
  icons/                     ícones gerados (`tauri icon` a partir do icon-512.png)
  binaries/node.exe          o Node embutido (gitignored — copiar do sistema antes de buildar)
```

**Fluxo (`src/lib.rs`):** no boot, o Rust roda `node server/index.mjs` com `CDC_DATA_DIR` = diretório de dados gravável (`app_data_dir`, ~%APPDATA%), espera a porta 5174 (`wait_port`) e cria a janela em `http://localhost:5174`. Ao fechar, o Node é morto. **Pegadinha resolvida:** o `resource_dir()` do Tauri no Windows retorna caminho *verbatim* (`\\?\C:\...`) que o Node não resolve (`EISDIR lstat 'C:'`) — a função `strip_verbatim` remove o prefixo.

**Backend adaptado** (`server/index.mjs` + `db.mjs`): flag `PACKAGED` (= `CDC_DATA_DIR` setado) desliga o live reload (watcher + snippet injetado) e não abre o navegador; o SQLite vai pra `CDC_DATA_DIR` (gravável) em vez de `server/data`; o `.env` é lido de `CDC_DATA_DIR/.env` se existir, senão do bundle.

**Como buildar:** instalar Rust + MSVC Build Tools (já presentes na máquina — o CONTEXTO antigo dizia que faltavam, mas já estão); copiar `node.exe` pra `src-tauri/binaries/`; `npm install` (traz o `@tauri-apps/cli`); `npm run tauri build` (release) ou `... build --debug` (iteração). Gera `src-tauri/target/.../bundle/nsis/Centro de Comando_0.1.0_x64-setup.exe`.

**Pendências/decisões conscientes:**
- O `.env` (senha + chave Gemini) hoje vai **dentro do bundle** como semente — ok pro uso pessoal local, mas pra distribuir o ideal é um setup no primeiro run (não embute credenciais). Rode `npm run setup` com a SUA senha antes de buildar.
- O `node.exe` (92 MB) é gitignored; quem clonar precisa copiá-lo de novo.
- Validado em `--debug`; o release final (`profile.release` com LTO/strip) gera um .exe menor.

**Site de download** (`site/index.html`): landing page pública e **responsiva (mobile-first)** que apresenta o app e tem o botão de download do instalador. Hospede junto com o `..._x64-setup.exe` (o link aponta pro arquivo ao lado).

## Roadmap até o .exe

**Empacotamento desktop: decidido — Tauri** (2026-06-17). Núcleo Rust, sem Node exposto ao frontend (menor superfície de ataque) — só falta instalar o toolchain Rust + build tools no Windows (não estavam instalados na máquina até o momento desta decisão). Eletron e NW.js foram descartados.

Pendências antes do .exe:
1. ~~Persistência real~~ ✅ resolvido na Fase 3a (SQLite via backend).
2. ~~Decidir framework de empacotamento~~ ✅ Tauri.
3. ~~Conectar o Assistente a uma IA de fato~~ ✅ resolvido na Fase 3b (Gemini).
4. ~~Acesso mobile robusto via PWA~~ ✅ resolvido na Fase 3c (manifest + SW + ícones). Ressalva: no celular via IP HTTP o SW não roda (precisa de HTTPS) — vide limitação de secure-context na seção da Fase 3c.
5. ~~Empacotar com Tauri consumindo o backend local (Fase 3d)~~ ✅ resolvido — .exe autônomo com Node embutido (sidecar). Ver "Arquitetura do executável".
6. Ícone, nome do app, splash, talvez abrir no boot do Windows.

## Arquivos da pasta

- `Centro de Comando.dc.html` — app principal.
- `support.js` — runtime do framework DC.
- `manifest.webmanifest`, `sw.js`, `icons/` — PWA (Fase 3c): manifesto, service worker e ícones. Ver seção "Arquitetura PWA" acima.
- `tools/gen-icons.mjs` — gera os ícones PNG a partir do logo, zero-deps (`npm run icons`).
- `server/` — backend local (Fase 3a): API + SQLite + autenticação. Ver seção "Arquitetura de backend" acima.
- `Canvas.dc.html`, `Canvas-2.dc.html` — templates vazios, provavelmente reservados pra novas telas/testes.
- `ideias/` — 8 prints de referência visual (dashboard, sidebar, calendário, cor, etc.) que guiaram o design.
- `screenshots/` — 2 prints de demonstração do estado atual.
- `.gitignore` — exclui `Timed out questions defaults.zip` (arquivo solto sem relação com o projeto), lixo de OS, pastas de build futuras e os segredos/dados do backend (`server/.env`, `server/data/*.sqlite`).
- `package.json` — scripts: `npm run dev` (servidor estático antigo, sem backend, útil pra ajuste visual rápido), `npm run setup` (define a senha inicial), `npm run server` (servidor principal a partir da Fase 3a — estáticos + API + auth, porta 5174), `npm run icons` (regenera os ícones do PWA).

## Plano de implementação em fases (decidido em 2026-06-17)

Ordem do mais simples/rápido pro mais difícil/complexo. Cada fase/sub-fase termina com checkpoint: reporto o que mudou e peço OK antes de seguir pra próxima.

**Fase 1 — correções rápidas e fundação de dados** ✅ concluída
**Fase 2 — CRUD completo e interações** ✅ concluída

**Fase 3 — assistente IA real, backend seguro, mobile e .exe** (dividida em sub-fases)
- **3a — Backend local + autenticação + migração de dados** ✅ concluída.
- **3b — Assistente IA real** (Gemini, function calling, confirmação obrigatória de toda ação, log de auditoria) ✅ concluída (esta seção).
- **3c — Acesso mobile** (PWA: manifest + service worker) ✅ concluída.
- **3d — Empacotamento Tauri (.exe)** consumindo o mesmo backend local ✅ concluída (Node embutido como sidecar) + landing page de download responsiva.
- *(fora do escopo por enquanto)* hospedagem pública (VPS vs PaaS) — só quando quiser acesso de fora da rede local; aí entra TLS real, domínio, hardening de servidor.

## Changelog

- **2026-06-17** — Repositório git inicializado (branch `main`), `.gitignore` criado, este `CONTEXTO.md` criado, commit inicial com o estado atual do projeto como baseline.
- **2026-06-17** — Lida a fundo a lógica do `.dc.html` e mapeados os gaps reais (seção "O que falta implementar" acima): zero persistência, assistente decorativo, bug no save de lançamento/ideia, sem editar/excluir, sem drag-and-drop no kanban, ⌘K decorativo.
- **2026-06-17** — Dev server local criado (`npm run dev`, porta 5174, live reload) e plano de implementação em 3 fases definido.
- **2026-06-17 — Fase 1 entregue:**
  - Persistência via `localStorage` (chave `cdc-exe-state-v1`): restaura estado ao abrir, salva a cada `setState`. Cobre clientes, prospectos, demandas, ideias, lançamentos e nós extras do workflow.
  - `lancamentos` e `ideias` migrados pro padrão `state` com fallback (`s.X || xDefault`), igual já era feito com `clients`/`demands`/`prospectos`.
  - `submitDrawer` ganhou os branches `lancamento` e `ideia` (antes só fechavam o drawer e mentiam que salvaram). Lançamento calcula label/cor/ícone pelo tipo (receita/despesa); ideia usa a categoria escolhida pra colorir a tag.
  - Drawer de ideia: título, categoria (chips Pessoal/Produto/Conteúdo/Cliente) e observações agora têm handlers reais (`ideiaTituloChange`, `ideiaObsChange`, `ideiaCatSetX`) — antes eram inputs sem `value`/`onInput`.
  - Botões "Nova ideia"/"Capturar ideia" corrigidos pra chamar `openDrawerIdeia` (antes chamavam `openDrawer`, abrindo no modo errado).
  - `clientCards` ganhou alerta de cliente atrasado: clientes fixos com `pagDia` vencido no mês (dia atual > `pagDia`) mostram status "Atrasado" em vermelho (`var(--crit)`).
  - Sintaxe do bloco JS validada via `new Function(...)` sem erros; testado rodando no dev server local (`npm run dev`, porta 5174) com live reload.
- **2026-06-17 — Fase 2 entregue:**
  - **Editar/excluir** em todas as 5 entidades (demanda, cliente, prospecto, lançamento, ideia). Novo campo `drawerEditId` no state: `null` = criando, `id` = editando — `submitDrawer` ramifica update vs. create pra cada modo. Cada card/linha ganhou botão de excluir (ícone X reaproveitado, sem ícone de lixeira no kit) com `window.confirm(...)` antes de remover.
  - **Demandas:** clicar num card (kanban por status ou lanes por cliente) abre o drawer já preenchido pra editar. Cards fixos da tela "Pessoal" (não fazem parte do state real) ficam não-editáveis via segundo parâmetro `mkLane(d, false)`.
  - **Lançamentos:** seed ganhou `id`/`tipo`/`valorRaw`/`dataRaw`; novo `buildLancamento(...)` compartilhado entre criar/editar evita parsear texto já formatado.
  - **Ideias:** seed ganhou `id`/`obs`; nova `ideiasView` (derivada de `ideias`) injeta os handlers de editar/excluir no template.
  - **Drag-and-drop real no kanban** (visão por status): `draggable`, `onDragStart` (card), `onDragOver`/`onDrop` (coluna) via HTML5 DnD nativo — solta o card numa coluna e o `status` da demanda muda de fato.
  - **Busca ⌘K funcional:** `Cmd/Ctrl+K` (listener global em `componentDidMount`, removido em `componentWillUnmount` novo) ou clique no badge do topo abrem um modal central; busca por título/nome em demandas, clientes e prospectos; clicar num resultado abre o drawer de edição certo e troca de tela; `Esc` ou clique no scrim fecha.
  - Sintaxe validada via `node --check` no bloco da classe `Component` (duas vezes, sem erros). Servido e confirmado via dev server local (porta 5174, live reload).
- **2026-06-17 — Fase 3a entregue (backend + autenticação + migração de dados):**
  - Novos arquivos: `server/index.mjs` (HTTP: estáticos + live reload + API), `server/db.mjs` (SQLite via `node:sqlite`, tabela `state_store`), `server/auth.mjs` (hash `scrypt`, sessão em cookie HTTP-only, rate limit por IP), `server/setup.mjs` (`npm run setup`, define a senha inicial), `server/.env.example` (comitado, só nomes de chave).
  - `Centro de Comando.dc.html`: novo gate de login (`showLogin`/`authed`/`loginPassword`/`loginError` no state) — nada do app renderiza sem sessão válida. `componentDidMount` agora chama `/api/me` e, se autenticado, `/api/state`; `setState` sobrescrito faz `PUT /api/state` debounced (400ms) em vez de gravar no `localStorage`.
  - `.gitignore`: adicionado `server/.env` e `server/data/*.sqlite`.
  - `package.json`: novos scripts `npm run setup` e `npm run server` (porta 5174, mesma de sempre); `npm run dev` (servidor antigo, sem backend) continua disponível pra ajustes visuais rápidos.
  - **Verificação completa (todos os 7 passos do plano), rodada ao vivo:** `npm run setup` gravou o hash em `server/.env`; `npm run server` subiu normalmente; 5 senhas erradas bloquearam a 6ª tentativa (429) e a senha certa logou (200 + cookie `HttpOnly`); `PUT`/`GET /api/state` foram e voltaram consistentes; **reiniciei o processo do servidor e os dados continuaram lá** (vêm do SQLite, não da memória) — e a sessão, como esperado, exigiu novo login após o restart; `/server/.env` retorna 403 se alguém tentar acessar pela URL; `git status --ignored` confirmou que `server/.env` e `server/data/` não são rastreáveis.
  - Removi um processo `node` antigo (`tools/dev-server.mjs`, sem backend) que tinha ficado rodando na porta 5174 de uma sessão anterior, pra liberar a porta pro novo `server/index.mjs`.
  - **Senha definida durante o teste: `teste123`** — troque com `npm run setup` se quiser outra (ele pergunta se você quer sobrescrever).
  - IP da sua rede local pra testar do celular (mesma Wi-Fi do PC): `http://192.168.1.2:5174/`.
- **2026-06-17 — Fase 3b entregue (Assistente IA real com ações executáveis):**
  - Novos arquivos: `server/tools.mjs` (15 function declarations + `summarizeAction`), `server/gemini.mjs` (chamada à API do Gemini via `fetch` nativo, function calling).
  - `server/db.mjs`: nova tabela `audit_log` (`logAction`/`updateActionStatus`/`listActions`).
  - `server/index.mjs`: novas rotas `POST /api/chat`, `POST /api/chat/resolve`, `GET /api/audit`, rate limit de 50 msgs/hora/IP pro chat.
  - `server/setup.mjs`: pergunta opcional da `GEMINI_API_KEY`, preservando a chave existente se você só trocar a senha.
  - `Centro de Comando.dc.html`: chat real (`aiSendMessage`, `aiApplyAction`, `aiConfirmAction`, `aiRejectAction`), card de ação com Confirmar/Cancelar, painel "Histórico de ações" na sidebar do Assistente.
  - Ver seção "Arquitetura do Assistente IA (Fase 3b)" acima pra fluxo completo e os dois desvios conscientes em relação ao plano original (modelo `gemini-3.5-flash` em vez de `gemini-2.5-flash`; header `x-goog-api-key` em vez de `?key=` na URL).
  - **Verificação rodada sem chave do Gemini configurada** (ainda não há uma cadastrada): servidor sobe normal; sintaxe de todos os arquivos (`server/*.mjs` e o bloco JS do `.dc.html`) validada via `node --check`; rotas novas (`/api/chat`, `/api/audit`) confirmadas retornando 401 sem sessão; login testado de verdade com a senha de teste (`teste123`, documentada acima) — funcionou, e `/api/chat` respondeu com o aviso amigável de "assistente não configurado" (sem travar nada); `summarizeAction` testado com os 15 tools contra dados de exemplo, todas as frases corretas; round-trip do `audit_log` (inserir → listar → atualizar status → listar) testado direto contra o SQLite real, sem deixar linha de teste para trás.
  - **Ainda pendente (depende de uma chave real do Gemini, que você ainda não tem):** testar uma pergunta de verdade, uma criação/edição/exclusão confirmada de ponta a ponta (aparecer na tela, persistir, e o `audit_log` virar `applied`), o caminho de Cancelar, e o comportamento ao pedir algo com um cliente/id inexistente. Assim que você gerar a chave (passo a passo no changelog enviado no chat) e rodar `npm run setup` de novo, posso testar tudo isso eu mesmo via terminal, sem precisar abrir o navegador.
- **2026-06-17 — Painel de demanda virou modal centralizado (era drawer lateral):**
  - `Centro de Comando.dc.html`: o painel de criar/editar demanda (antes um drawer deslizando da direita, `position:absolute; right:0`) agora usa o mesmo padrão do painel de cliente/prospecto/lançamento/ideia — centralizado na tela (`top:50%; left:50%; transform:translate(-50%,-50%)`), cantos arredondados, header com gradiente, scrim com blur de 3px (igual ao painel de cliente, era 2px). Padding do corpo e do rodapé ajustados de 20px pra 24-28px pra acompanhar o outro painel.
  - **Bug encontrado e corrigido na animação `cc-pop`:** ela define `transform: scale(...)` em cada keyframe, o que **sobrescreve por completo** qualquer `transform` estático no elemento (CSS não combina os dois — animação vence). Isso fazia o `translate(-50%,-50%)` virar `translate(0,0)` assim que a animação rodava, deixando o canto superior-esquerdo do painel no centro da tela em vez do painel inteiro centralizado — **esse bug já existia no painel de cliente/prospecto/lançamento/ideia também**, só não tinha sido notado. Corrigido incorporando o `translate(-50%,-50%)` em cada keyframe do `cc-pop` (linha do `<style>` no topo do arquivo). Os dois painéis (demanda e cliente/etc.) usam esse keyframe e foram corrigidos juntos.
  - **Verificação:** subi o backend real (`npm run server`, login com a senha de teste `teste123`), abri "Nova demanda" via Playwright (Chromium headless, instalado só pra esse teste e removido depois) e confirmei por medição de `getBoundingClientRect()` que o centro do painel cai exatamente no centro do viewport (não só visualmente — calculei: x=720 de 1440, igual à metade exata). Screenshot confirmou fundo desfocado e painel arredondado, igual ao painel de cliente.
  - **Não tocado (fora do escopo, mas mesmo bug):** o modal de busca ⌘K (`animation:cc-drawer`, linha ~1244) usa `transform:translateX(-50%)` com a mesma lógica de animação por keyframe — provavelmente tem o mesmo tipo de bug (a animação `cc-drawer` termina em `translateX(0)`, sobrescrevendo o `-50%`). Não mexi porque não foi pedido, mas é um candidato óbvio pra próxima limpeza visual.
- **2026-06-17 — Revertido: demanda voltou a ser drawer lateral (decisão do usuário após ver o resultado centralizado):**
  - `Centro de Comando.dc.html`: desfeitas as mudanças de estrutura/padding do item anterior só pro painel de demanda — voltou a ser `position:absolute; top:0; right:0; bottom:0; width:min(480px,82%)`, scrim `rgba(.55) blur(2px)`, padding `20px`/`16px 20px`, `animation:cc-drawer`. **Mantido:** o fix do keyframe `cc-pop` (continua beneficiando cliente/prospecto/lançamento/ideia, que seguem centralizados) e a divisão em dois blocos `sc-if` separados (`isLateralDrawerOpen` pra demanda, `isEntityPanelOpen` pros outros 4).
- **2026-06-17 — QA completo de todas as telas e interações (a pedido do usuário):**
  - Suite automatizada via Playwright/Chromium headless (script descartável, não commitado) contra o backend real (`npm run server`, login `teste123`), cobrindo: login, dashboard Início, abrir/preencher/criar/editar/excluir demanda (confirmando que abre como drawer lateral encostado na direita), kanban "Por status" e drag-and-drop real entre colunas (simulado via `DragEvent`/`DataTransfer` nativos, já que Playwright não dispara HTML5 DnD nativamente), Clientes (confirmando painel centralizado, criar/excluir), Ideias (criar/excluir), busca ⌘K, Financeiro (modal de lançamento centralizado), Assistente (mensagem real enviada e respondida pela API do Gemini em ~1s, com resposta contextual correta sobre os dados reais do app — confirma que a Fase 3b está 100% funcional, não só "não configurado" como nas verificações anteriores sem chave), Workflow (adicionar nó), Calendário, Salário, Metas, Pessoal, recolher/expandir sidebar.
  - **Resultado: 28/28 passos OK, zero erros de console/página.** Único problema encontrado durante os testes (não do app, do meu script): o modal de Lançamento não fecha com `Esc` — só o modal de busca tem esse atalho — então fechar com `Esc` deixa o scrim bloqueando cliques subsequentes; troquei pra fechar clicando em "Cancelar". Isso não é um bug do app, é uma limitação real (Esc não fecha os modais de cliente/prospecto/lançamento/ideia/demanda, só o de busca) — se quiser, posso adicionar esse atalho nos outros modais.
  - Dados de teste ("QA Cliente Teste", "QA Teste - demanda automatizada") ficaram residuais no banco real depois de uma primeira rodada que travou no meio (bug no meu script de teste, dois listeners de diálogo duplicados); limpos manualmente depois via `PUT /api/state` direto na API, sem tocar nos dados reais do usuário (`Cliente Teste`, `jet ski lanchas`, `Demanda Teste`, `ada`, `fs`, `sgsg` continuam intactos).
- **2026-06-17 — Mais motion/interatividade (a pedido do usuário):**
  - `Centro de Comando.dc.html`: novos keyframes `cc-rise` (entrada sutil fade+translateY, usada em listas/cards), `cc-dot` (pulso pra indicador de "pensando"), `cc-search-pop` (entrada do modal de busca).
  - **Stagger reveal** (entrada escalonada por item, via `{{ $index }}` do `sc-for` multiplicado em `animation-delay`) nos: KPIs e listas da tela Início (demandas de hoje/futuras/atividades), cards do kanban "Por status" e "Por cliente", lista de clientes, grid de ideias, resultados da busca ⌘K.
  - **Feedback visual de drag-and-drop no kanban:** card arrastado fica com opacidade reduzida (`draggingId` no state); coluna de destino ganha fundo azulado + borda pontilhada accent enquanto o mouse está sobre ela (`dragOverCol` no state, via `onDragEnter`/`onDragLeave` novos). Limpa tudo em `onDragEnd`/`onDrop`.
  - **Feedback de clique (`style-active`, pseudo-classe genérica já suportada pelo framework DC)** nos botões de CTA principais (accent, 13 ocorrências), nos itens da sidebar, nos chips de prioridade da demanda, no checkbox do checklist e nos resultados da busca — tudo com leve `scale()` ao pressionar.
  - **Indicador ativo da sidebar** ganhou transição suave de cor/fundo (`transition:background .16s ease, color .16s ease`) em vez de troca instantânea ao navegar entre telas.
  - **"Pensando…" do Assistente** virou 3 pontinhos pulsando (`cc-dot`, defasados 150ms entre si) no lugar do texto estático.
  - **Bug de centralização corrigido no modal de busca ⌘K:** mesmo problema do `cc-pop` (animação `cc-drawer` sobrescrevia o `translateX(-50%)`, deixando a busca desalinhada à esquerda do centro). Criei `cc-search-pop` dedicado (não reaproveitei `cc-drawer` porque esse keyframe também serve o drawer lateral de demanda, que precisa do efeito de slide original sem nenhum translate fixo — misturar os dois quebraria um ou outro).
  - **Verificação:** suite Playwright completa re-executada após as mudanças (login, demanda lateral + criar/arrastar/editar/excluir, cliente centralizado + criar/excluir, busca ⌘K agora com dx=0 do centro exato, assistente respondendo) — **5/5 OK, zero erros de console**. Screenshot confirmou visualmente o destaque azul na coluna de destino durante o drag e os pontinhos animados no chat.
- **2026-06-18 — Bug raiz encontrado: hover não funcionava em lugar nenhum do app (apesar de já existirem 79 usos de `style-hover` no código):**
  - **Causa:** o framework DC (`support.js`) gera o CSS de `style-hover`/`style-active`/`style-focus` como uma classe comum (`.scpN:hover{...}`), **sem `!important`**. Como cada card já define a mesma propriedade (`background`, `border-color`, `box-shadow`, `transform`...) direto no atributo `style=""` inline, e estilo inline sempre vence regra de classe em CSS (não importa pseudo-classe nem especificidade), a troca no hover era calculada mas nunca chegava a ser desenhada — confirmado com Playwright medindo `getComputedStyle` antes/depois do hover em vários elementos (cor de fundo do botão "Nova demanda" não mudava nunca, por exemplo). Em cards com animação de entrada (`animation:cc-rise ... both`), o problema é ainda mais forte: o estado final da animação tem prioridade maior que estilo inline normal, então mesmo se a regra de hover não tivesse esse problema, a animação sozinha já travaria o `transform` no valor final.
  - **Correção (uma linha, na raiz, beneficia o app inteiro):** em `support.js`, função `createPseudoSheet` (gera as classes `.scpN:hover` etc.), cada declaração do CSS gerado agora recebe `!important` antes de ser inserida na stylesheet. Pela ordem de prioridade do CSS (important-author > animation > normal-author/inline), isso faz a regra de hover vencer tanto o estilo inline normal quanto o estado "congelado" das animações de entrada — sem precisar editar nenhum dos 79+ usos existentes de `style-hover` no `.dc.html`.
  - **Verificado com Playwright (medindo `getComputedStyle` real antes/depois do `.hover()`, não só visual):** botão "Nova demanda" (`background-color`: `rgb(82,133,240)` → `rgb(63,111,224)`, mudou); card de KPI da tela Início (`transform`: `matrix(1,0,0,1,0,0)` → `matrix(1,0,0,1,0,-3)`, e `box-shadow`: sh-1 → sh-2, ambos mudaram mesmo com a animação `cc-rise` ativa). Sintaxe de `support.js` e do bloco JS do `.dc.html` validada (`node --check` / `new Function`).
- **2026-06-18 — Mais interação visual no hover, a pedido do usuário ("quero que mexa nos cards, nas demandas, tudo ao passar o mouse tenha interação"):**
  - Cards que já tinham hover sutil (borda + 1px) ganharam um "lift" mais perceptível: sobe 3px e ganha sombra mais forte (`var(--sh-2)`) — demandas de hoje, kanban (por status e por cliente), clientes, prospectos, ideias, projetos pessoais.
  - Cards que **não tinham hover nenhum** ganharam: KPIs da tela Início, KPIs do Financeiro, cards de Metas (sobem e ganham sombra); linha de "Atividades recentes", linha do breakdown de Salário, célula do Calendário (ganham destaque de fundo/leve zoom); linha de "Lançamentos" do Financeiro e linha de "Demandas futuras" (ganham destaque de fundo + leve deslocamento lateral).
  - Nó do Workflow: hover ficou mais forte (sobe mais, ganha sombra de janela `var(--sh-3)` em vez de só mudar a borda).
  - **Verificação:** suite Playwright (com `getComputedStyle` antes/depois do `.hover()` real, não só screenshot) confirmando que `transform`/`box-shadow`/`background` de fato mudam nos elementos testados. Scripts de teste descartáveis removidos depois, junto com a dependência `playwright` que foi instalada só para esse teste (`npm install --no-save`) e desinstalada ao final — `package.json`/`node_modules` voltaram ao estado original.
- **2026-06-19 — Fase 3c entregue (PWA: app instalável):**
  - Novos arquivos: `manifest.webmanifest` (raiz), `sw.js` (service worker, raiz), `icons/` (icon.svg + icon-192/512 + icon-maskable-512 + apple-touch-icon), `tools/gen-icons.mjs` (gerador de ícones zero-deps).
  - `Centro de Comando.dc.html`: tags PWA no `<head>` estático (manifest, theme-color `#0d0e10`, apple-touch-icon, `apple-mobile-web-app-*`, favicon SVG, `viewport-fit=cover`, `<title>`) + registro defensivo do service worker (`if ('serviceWorker' in navigator)` no `load`).
  - `server/index.mjs`: só uma linha — MIME `.webmanifest` → `application/manifest+json`. O resto (ícones, sw.js, manifest) já é servido pela rota de estáticos.
  - `package.json`: novo script `npm run icons`.
  - **Ícones zero-deps:** `tools/gen-icons.mjs` desenha o logo do app e escreve PNGs reais só com `node:zlib` (encoder PNG próprio com CRC32 + IHDR/IDAT/IEND e antialias 4×4) — sem instalar nada. Reproduz fielmente o quadrado de gradiente azul + quadrado branco central.
  - **Service worker network-first**, blindado contra `/api/*` e `/__livereload` (passam direto). Cacheia o app shell + React/ReactDOM (unpkg) + fontes (Google) em runtime. Detalhes e fluxo na seção "Arquitetura PWA (Fase 3c)" acima.
  - **Limitação documentada:** SW e instalação automática (Android/Chrome) exigem HTTPS ou localhost. Funciona 100% no desktop em `localhost`; no celular via IP HTTP o SW não roda (precisa de HTTPS — virá com o Tauri da 3d ou hospedagem com TLS), mas o iOS já abre standalone via as meta tags apple. Vide a seção da Fase 3c.
  - **Verificação ao vivo (Playwright/Chromium headless já em cache, contra o `npm run server` real, login `teste123`):** SW registra (escopo `/`) e ativa; após reload controla a navegação; app renderiza e loga com o SW no caminho; `/api/me` responde `{ok:true}` fresco (não interceptado/cacheado); cache `cdc-pwa-v1` com 11 itens (shell + React + fontes) e zero rotas `/api`/`__livereload`; manifest válido (4 ícones, any + maskable). Zero erro de console. Os 4 PNGs validados à parte (assinatura, IHDR color-type 6, IDAT no tamanho exato). Tudo de teste removido ao final (working tree limpo).
- **2026-06-19 — QA funcional completo + reset dos dados de teste (a pedido do usuário):**
  - **QA (Playwright contra o server real):** 21/21 OK, **zero erros de console** — login, as 11 telas, tabs de demanda, calendário mês/semana, busca ⌘K, hover (fix do `support.js`), sidebar recolher/expandir, criar+editar demanda, criar ideia, e o Assistente respondendo de verdade (Gemini). Também confirmado que a navegação e os empty states funcionam com o banco vazio.
  - **Reset "app limpo":** zerados todos os dados (`clients`/`demands`/`prospectos`/`ideias`/`lancamentos`/`wfNodesExtra` → listas vazias) e limpo o `audit_log`, via script direto no SQLite com o servidor parado. Backup completo (sqlite + state.json + audit.json) guardado **fora do repo** em `C:\Users\patri\cdc-backups`. Os dados de teste eram: clientes `Cliente Teste`/`jet ski lanchas`, demandas `Demanda Teste`/`ada`/`fs`/`sgsg`, 6 ideias de exemplo, e 3 ações no audit.
  - **Descoberta:** a tela Início e os badges da sidebar são hardcoded (ver aviso na seção "Tema claro/escuro") — o reset não os afeta.
- **2026-06-19 — Tema claro/escuro alternável (a pedido do usuário "coloque em versão clara tb, pra poder ser alterado"):**
  - `Centro de Comando.dc.html`: blocos `:root[data-theme=dark/light]` no `<head>` estático com a paleta completa + novas vars (`--app-bg`, `--app-gradient`, `--scrollbar`, `--wire`); anti-flash script; botão ☾/☀ no titlebar; ícones `sun`/`moon`; handler `toggleTheme`; `componentDidMount` sincroniza o `mode`; helmet e root div ajustados pra usar as vars; fios do Workflow via `--wire`. Ver seção "Tema claro/escuro" acima.
  - **Verificação (Playwright):** alterna dark↔light, **persiste no reload**, cores batem com a paleta (body `#060708`↔`#e4e7eb`, texto `#e9eaec`↔`#1b1f27`), dark intacto após a refatoração, navegação e empty states OK no claro, **zero erros de console**. Screenshots dos dois temas conferidos. Temporários e `playwright` (--no-save) removidos ao final.
- **2026-06-19 — Tela Início conectada ao state + badges dinâmicos (a pedido do usuário, pós-reset):**
  - A Início era 100% hardcoded (KPIs `12/8/R$6.450`, "Luso Automóveis", "Flamenguismo 90+") e não respondia ao reset. Agora os 4 KPIs ("Demandas ativas", "Entregas", "A receber", "Vencendo hoje"), as listas "Demandas de hoje" e "Demandas futuras", e o subtítulo do header **derivam de `s.demands`/`s.clients`/`s.lancamentos`**. "Atividades recentes" fica em empty state (sem fonte real ainda).
  - **Empty states** novos nas 3 seções quando vazias ("Nenhuma demanda pra hoje/futura", "Nenhuma atividade recente"); subtítulo vira "Nenhuma demanda cadastrada ainda — comece criando uma."
  - **Badges da sidebar** (`Demandas`/`Clientes`) agora são a contagem real do `state` e **somem quando 0** (eram fixos `12`/`5`).
  - **Verificação (Playwright):** com o banco vazio → todos os KPIs em 0/R$0,00, os 3 empty states presentes, sem "Luso/Flamenguismo", badges sumidos; ao injetar 1 demanda → aparece em "Demandas futuras", empty state some, badge vira "1". Zero erros de console. Screenshot da Início vazia conferido. Estado deixado vazio (limpo) ao final.
- **2026-06-19 — Melhorias de design/animação (a pedido do usuário, aplicando as diretrizes de frontend-design):**
  - **Morph de tema:** o toggle claro↔escuro agora faz uma transição suave de cores (classe `.theming` ligada só durante a troca, ~480ms) em vez de trocar instantâneo. Não afeta hovers/performance no resto.
  - **Login com stagger reveal:** logo → título → input → botão entram escalonados (cc-rise, easing `cubic-bezier(.16,1,.3,1)`) em vez de tudo de uma vez.
  - **Data e saudação reais:** o header da Início era fixo "Segunda · 15 de junho" / "Boa tarde, Rafael"; agora deriva de `new Date()` (ex: "Sexta · 19 de junho" / "Boa noite" por faixa de hora).
  - **Assistente coerente:** a mensagem de boas-vindas virou genérica/dinâmica (saudação por hora, sem "Reels do Flamenguismo"); os 3 chips de "Contexto atual" (eram fixos "Reels vence em 1h" / "4 demandas hoje" / "R$ 2.250 recebidos") agora **derivam do state** (demandas pra hoje, ativas/entregues, clientes/prospects); a sugestão rápida do Flamengo virou genérica.
  - **Descoberta (durante a análise):** o `fs.watch` do dev server observava `tools/`, então cada screenshot do Playwright salvo ali disparava live reload e "voltava" a navegação pra Início — não era bug do app. Testes passaram a salvar screenshots fora do projeto.
  - **Ainda hardcoded (escopo de dados, não design):** Financeiro (KPIs + gráfico de 6 meses), Salário, Metas e a notificação do sino ainda têm dados de exemplo — coerentes visualmente, mas com valores fixos. Conectar como foi feito com a Início/Assistente.
  - **Verificação (Playwright, screenshots fora do dir observado):** morph (classe liga/desliga, tema troca), login renderiza, data real na Início, Assistente com saudação dinâmica + chips derivados, tema claro coerente em todas as telas (Início/Demandas/Clientes/Financeiro/Workflow conferidos), fios do Workflow via `--wire` OK nos dois temas. **Zero erros de console.** Estado deixado vazio ao final.
- **2026-06-20 — Fase 3d entregue (.exe autônomo via Tauri + Node embutido) + site de download:**
  - Novo `src-tauri/` (projeto Tauri 2): `lib.rs` inicia o Node sidecar (`node server/index.mjs`) com `CDC_DATA_DIR`, espera a porta 5174 e abre a janela; mata o Node ao fechar. `tauri.conf.json` (janela via código, resources do app + node.exe, bundle NSIS, ícones). Ver "Arquitetura do executável".
  - `server/index.mjs` + `db.mjs`: flag `PACKAGED` desliga live reload e o auto-open do navegador; SQLite e `.env` passam a usar `CDC_DATA_DIR` (gravável).
  - `package.json`: devDep `@tauri-apps/cli`, script `npm run tauri`. `.gitignore`: `src-tauri/target|gen|binaries`.
  - **Bug resolvido:** `resource_dir()` do Tauri retorna caminho verbatim (`\\?\C:\...`) que o Node não roda (`EISDIR lstat 'C:'`) → `strip_verbatim` no `lib.rs`.
  - **Verificação ao vivo:** build gerou `.exe` + instalador NSIS (`Centro de Comando_0.1.0_x64-setup.exe`, ~26 MB); rodando o `.exe`, confirmado o processo do `.exe` + o **Node filho** (sidecar) servindo a porta 5174 sozinho, **sem servidor à parte**; o servidor responde no modo packaged (sem snippet de live reload); janela abre com o app. Tela de login conferida (Playwright contra o sidecar).
  - **`site/index.html`** — landing page de download **responsiva** (testada em desktop 1300px e mobile 390px): hero, mockup do app, 6 features, CTAs de download. Aponta pro instalador ao lado.
  - **Senha de teste atual no `.exe`: `teste123`** (vem do `server/.env` do bundle; troque com `npm run setup` antes do build final).
- **2026-06-20 — Correção de bugs críticos de uso + perfil de cliente + Workflow "cérebro" (a pedido do usuário, que estava travado):**
  - **Bug 1 — escrita travada:** os inputs de cliente/prospecto/ideia/lançamento não aceitavam digitação. Causa: o render expunha os *handlers* (`clienteNomeChange`…) mas **não os valores** (`clienteNome`…), então o React controlava o input com value vazio fixo. Corrigido expondo todos os valores. (Demanda e chat funcionavam porque já estavam expostos.) Testes antigos passavam por usar `page.fill` (que mascara o bug); só digitação real revela.
  - **Bug 2 — demanda some ao criar:** era salva mas o board abria "Por cliente" (padrão) e demandas sem cliente não tinham lane. Corrigido: padrão agora é "Por status" + nova aba **"Avulsas"** (demandas sem cliente) no board por cliente.
  - **Service worker só no `.exe`:** em dev o servidor devolve 404 pra `/sw.js` e o app limpa SWs antigos — evita o cache do PWA servir versão velha e confundir durante o trabalho.
  - **Perfil de cliente** (`ficha`): clicar num cliente abre uma ficha detalhada (header com avatar/nome/tags, stats de demandas + recebido, dados cadastrais, lista de demandas do cliente), com Voltar/Editar/Excluir/Nova demanda. Inspirado na ref em `ideias/`.
  - **Workflow virou um "cérebro" (grafo estilo Obsidian):** em vez do fluxo linear fixo, agora um grafo radial derivado do `state` — centro "Minha agência" → clientes (+ grupos "Avulsas" e "Ideias") → demandas/ideias como folhas, conectados por linhas. Atualiza automático conforme surgem demandas. Nós clicáveis (abrem o cliente/demanda). **Pegadinha do DC:** ele não interpola `{{ }}` em atributos `<line>` SVG — as conexões viraram divs finos rotacionados (interpolados via `style`).
  - **QA (Playwright, digitação real):** todas as criações funcionam (demanda/cliente/prospecto/ideia/lançamento/chat); perfil abre com dados e demandas do cliente; cérebro monta com centro+grupos+folhas e zero erros. Estado deixado vazio.
- **2026-06-20 — Drawer de nova demanda repaginado + aba Demandas (a pedido do usuário):**
  - **Cliente inline:** botão "+ novo cliente" no próprio drawer abre um campo pra criar e já selecionar o cliente (Enter confirma), sem fechar/perder a demanda em edição. Continua tendo o select normal ("× cancelar" volta pra ele).
  - **Prazo funcional:** o "Hoje · 17:00" estático virou `input[type=date]` + `input[type=time]` reais; salva em `dueDate`/`dueTime` e gera rótulo amigável (Hoje/Amanhã/DD mmm · HH:MM) via `fmtDue`. Ao editar a demanda os campos repopulam.
  - **Tipo "Gravação":** adicionado o chip (type key `rec`). **Importante:** os chips de tipo viraram **seleção única** (antes eram multi e o submit só salvava o 1º ativo → escolher Gravação não tinha efeito). Agora o tipo escolhido é o salvo.
  - **Checklist:** default passou a ser só "Aprovação do cliente". Cada item agora é **editável** (input) e **excluível** (×), além de marcar/adicionar. Salvo como `checklist:[{label,done}]` na demanda.
  - **Anexos reais:** a caixa virou `input[type=file] multiple` de verdade; nomes viram chips removíveis e vão pra `attachments` da demanda. (Guardamos só os nomes, não o conteúdo — suficiente pra referência; dá pra evoluir pra dataURL depois.) **Pegadinha DC:** atributo booleano `multiple` é descartado — usar `multiple="multiple"`.
  - **Guarda de saída + Limpar:** fechar o drawer (scrim/×/Cancelar) com campos preenchidos e não salvos pede confirmação ("não saia sem salvar"). Botão **Limpar** zera tudo sem fechar. Briefing agora **salva** (antes a textarea não tinha binding).
  - **Aba Demandas — arrastar p/ trocar cliente:** na view "Por cliente", cards são arrastáveis e as **abas de cliente viram drop targets** — soltar um card numa aba reatribui o `clientId` (aba "Avulsas" tira o cliente). Realça a aba no hover de drag.
  - **Aba Demandas — prioridade com cores:** a demanda guarda `priority` (baixa/média/alta); cards mostram **borda esquerda + badge colorido** (verde/âmbar/vermelho) nas duas views; o botão "Filtrar" decorativo virou **filtro segmentado por prioridade** (Todas/Alta/Média/Baixa) que filtra board e lanes.
  - **QA (Playwright, digitação real):** 24 checagens passando — criação completa (título, cliente inline, prazo 25 jun · 14:30, Gravação, Alta, briefing, checklist editar/add/excluir, 2 anexos), badge de prioridade, filtro (1 alta / 1 baixa / 2 todas), drag trocando cliente, popup de guarda de saída e Limpar. Zero erros de JS (404 de `/sw.js` e favicon são esperados em dev). Estado deixado vazio.
- **2026-06-20 — Editar demanda clicando nela (Início) + opção Excluir no drawer:**
  - No Início, os cards de "Demandas de hoje" e as linhas de "Demandas futuras" chamavam `openDrawer` (abriam um drawer **em branco**). Agora chamam `editDemanda(d)` → abrem a demanda **em modo edição** (título/prazo/tipo/prioridade/checklist/anexos preenchidos), cabeçalho "Editar demanda" e CTA "Salvar alterações". `_tLabelHome`/`_tIconHome` ganharam `rec` (Gravação).
  - **Rodapé do drawer agora é contextual:** criar = `Salvar`/`Criar demanda` + **Limpar** + Cancelar; editar = **Salvar alterações** + **Excluir** (com confirm) + Cancelar. O rótulo pequeno do topo também reflete "Editar …".
  - Detalhe técnico: `editDemanda` é referenciado em closures dos builders do Início que são declarados antes dele no `render()` — ok porque só executam no clique (já inicializado).
  - **QA (Playwright):** 11 checagens — Início mostra hoje/futura, clique abre em edição com título preenchido, CTA "Salvar alterações", botão "Excluir" presente e "Limpar" ausente no editar; edição salva; excluir pelo drawer remove. Zero erros. Estado deixado vazio.
- **2026-06-20 — Calendário ligado às demandas reais (mês + semana) com clique pra editar/criar:**
  - O calendário usava eventos **fixos (mock)**. Agora os eventos vêm das **demandas reais por `dueDate`** — chips coloridos pela cor do cliente (ou pela prioridade, quando sem cliente). `_iso(y,m,d)` monta a data; `calMonthEvents` (mês) e por-dia (semana) são derivados de `s.demands`.
  - **Clicar no chip da demanda → abre em edição** (`editDemanda`, com `e.stopPropagation()` pra não disparar o create do dia). **Clicar num dia vazio → cria** uma demanda já com aquela data (`openDemandaForDate(iso)` preenche `drawerDemandDate`).
  - **Semana virou dinâmica:** antes era fixa em 15–21 jun. Agora é a semana (Seg–Dom) de hoje, deslocável por `calOffset` (em semanas). Mês continua deslocando por meses. Trocar de view zera o `calOffset`; botão **Hoje** volta pra `calOffset:0`. Cabeçalho mostra "Mês AAAA" ou o intervalo "DD mmm – DD mmm".
  - **Legenda** deixou de ser "Luso/Flamenguismo" fixos — lista os clientes reais (até 6) com suas cores. "Novo evento" → "Nova demanda".
  - **QA (Playwright):** 8 checagens — mês mostra as 3 demandas; clicar chip edita (título certo); clicar dia 25 cria com `2026-06-25`; semana mostra a demanda + hora; clicar evento edita; clicar coluna Seg cria com `2026-06-15`. Zero erros. Estado deixado vazio.
- **2026-06-20 — Sidebar: seções colapsáveis + olho de ocultar valores (privacidade):**
  - **Seções colapsáveis:** os rótulos "Trabalho" e "Negócio" da sidebar viraram botões com **setinha** (chevron) que expande/retrai os itens. Estado `navWorkOpen`/`navBizOpen` (default expandido, `!== false`); chevron rotaciona (0° aberto / -90° fechado); os itens ficam num `sc-if` (`workItemsShown`/`bizItemsShown` = `s.collapsed || aberto`, então na sidebar minimizada os ícones continuam aparecendo).
  - **Olho de mostrar/ocultar valores** nas abas de Negócio (Financeiro/Salário/Metas): botão com ícone `eye`/`eyeOff` no header de cada uma; estado `hideMoney` **persistido em `localStorage` (`cdc-hideMoney`)**. Com ele ligado, `this.brl()` retorna `R$ ••••` — mas **só quando a tela atual é de negócio** (`['financeiro','salario','metas']`), então Início/perfil de cliente não são afetados (escopo). Valores fora do `brl` também mascarados: rótulos do gráfico de receita (`finBars`), `R$ 1.480,00` do Salário (virou binding `salRecomendacao`) e os campos de dinheiro da meta Faturamento. Contagens não-monetárias (8/15, 32h…) seguem visíveis.
  - Ícones novos no set: `eye`, `eyeOff`. Estado inicial também teve `drawerTypeActive`/`drawerChecklist` atualizados pros novos defaults (8 tipos / só "Aprovação do cliente").
  - **QA (Playwright):** 13 checagens — Negócio e Trabalho retraem/expandem; Financeiro mostra 11 valores → oculta (5 mascarados, 0 visíveis) → mostra; Salário e Metas mascaram junto (olho compartilhado); contagem "15" segue visível; `localStorage` persiste "1"; Início **não** mascara (escopo). Zero erros.
- **2026-06-20 — Campos de dinheiro só aceitam escrita numérica:**
  - Os inputs de **valor do cliente** e **valor do lançamento** agora bloqueiam letras/símbolos via `sanitizeMoney` (só dígitos + **uma** vírgula decimal). O **dia de pagamento** usa `sanitizeDia` (só dígitos, 2 chars, limitado a 31). Todos ganharam `inputmode` (`decimal`/`numeric`) pro teclado numérico no mobile.
  - Corrigida uma inconsistência: o valor do cliente era parseado com `parseFloat(_cp.valor)` (não entendia vírgula) — agora faz `.replace(',', '.')` como o lançamento já fazia, então `1800,50` vira `R$ 1.800,50` no perfil. Placeholder do cliente passou de "1.800,00" pra "1800,00" (sem ponto de milhar, já que o ponto é descartado).
  - **QA (Playwright):** `ab1.800,5x0z` → `1800,50`; `12,3,4` → `12,34` (uma vírgula); dia `a9b9` → `31` (cap); perfil mostra `R$ 1.800,50`; lançamento `R$ 2.450,9abc` → `2450,9`. Zero erros.
- **2026-06-20 — Assistente IA: verificado funcional + ferramentas alinhadas ao modelo novo:**
  - O Assistente (Fase 3b, Gemini `gemini-2.5-flash`) **já estava funcional** — a chave `GEMINI_API_KEY` no `server/.env` é válida. Confirmado E2E: enviar mensagem → resposta; "cria um cliente…" → card "Ação proposta" → Confirmar → ✅ Aplicado → cliente realmente criado; Q&A ("quantos clientes?") responde com o dado real.
  - **Lacunas fechadas** (a Fase 3b é anterior às melhorias de demanda): em `server/tools.mjs` a tool de demanda ganhou tipo **`rec` (Gravação)**, prioridade **`baixa`** (antes só alta/media), e os campos **`prazo` (AAAA-MM-DD) + `hora` (HH:MM)**; `summarizeAction` mostra prioridade/prazo. No frontend, `aiApplyAction` passou a gravar `priority`, `dueDate`/`dueTime`/`due` (via `fmtDue`) e o checklist default ao criar/editar via IA. **Exige reiniciar o `node server/index.mjs`** pra valer (o servidor importa TOOLS no boot).
  - **QA (curl + Playwright):** "cria uma demanda de gravação … prioridade baixa, prazo 28/06/2026 15h" → `create_demanda {type:rec, priority:baixa, prazo:2026-06-28, hora:15:00}`; confirmando, a demanda fica com `type:rec, priority:baixa, dueDate:2026-06-28, dueTime:15:00, due:"28 jun · 15:00"`. Zero erros.
  - **Nota p/ o `.exe`:** a `GEMINI_API_KEY` vai no bundle (server/.env) — tradeoff já documentado; chave extraível do instalador. Decidir na hora do build se entra ou fica de fora.
- **2026-06-20 — Aba Pessoal deixou de ser mock (item 3 da sequência 4→3→2→1):**
  - "Projetos ativos" eram 3 cards fixos; agora são as **demandas reais sem cliente** (`demands.filter(d => d.clientId == null && d.status !== 'done')` via `mkLane(d, true)`) — clicáveis pra editar, com borda de prioridade e excluir. "Demandas com cliente" não aparecem aqui (ficam no board). "Novo projeto" abre o drawer de demanda sem cliente.
  - "Ideias rascunho" eram 3 fixas; agora são as **ideias reais** (`ideias.map(...)`), clicáveis pra editar (painel da ideia), com **"Virar projeto"** (abre nova demanda já com título/obs da ideia via `projetoFromIdeia`) e excluir.
  - **Pegadinha:** o builder de `personalIdeas` precisou ir pra **depois** de `ideias`/`editIdeia` no `render()` (estavam definidos mais abaixo). E havia um `deleteIdeia` já existente — reusei (closure resolve no clique).
  - **QA (Playwright):** projeto pessoal aparece e o de cliente não; ideia aparece; clicar projeto → "Editar demanda"; clicar ideia → painel "Editar ideia"; "Virar projeto" → nova demanda com o título da ideia, confirmando cria demanda sem cliente. Zero erros.
- **2026-06-20 — Telas de Negócio deixam de ser mock (item 2 da sequência — calculadas dos dados reais):**
  - **Agregados** (após `lancamentos`): `recMes`/`despMes` = soma de receitas/despesas do mês atual (`dataRaw` no mês de hoje); `resultado = recMes − despMes`; `esperadoMes` = soma do valor dos clientes **fixos**; `aReceber = max(0, esperado − recebido)`.
  - **Financeiro:** os 4 KPIs e o gráfico "receita dos últimos 6 meses" (`finBarsRaw` montado por mês real) agora vêm dos lançamentos. (Os lançamentos da lista já eram reais desde a Fase 2.)
  - **Salário:** `base = rec − desp`; reserva 20%; **disponível = base×0,8**; **recomendação = disponível×0,9**. O número grande (`salRecomendacao`) e o donut (`salPct = recom/recebido`) viraram dinâmicos; histórico = disponível dos últimos 5 meses.
  - **Metas:** Faturamento (recebido vs. esperado dos fixos), Entregas (demandas done / total), Clientes ativos (clientes / clientes+prospectos), Prospecções (prospectos / meta 3) — tudo com % e notas reais. A antiga "Horas produtivas" (sem fonte de dados) saiu; entrou "Prospecções".
  - Tudo respeita o **olho de privacidade** (os valores em `brl()` mascaram nas telas de negócio).
  - **QA (Playwright, dados semeados):** rec 1500+500 e desp 300 → Financeiro recebido R$ 2.000 / a receber R$ 1.000 / despesas R$ 300 / resultado R$ 1.700, esperado R$ 3.000; Salário reserva 340 / disponível 1.360 / recomendação 1.224 / donut 61%; Metas faturamento R$ 2.000, entregas 2/3=67%, clientes 1/2=50%; olho mascara. Zero erros.
- **2026-06-20 — Chave do Gemini configurável no app (decisão: o `.exe` vai ser DISTRIBUÍDO, então a chave não pode ir no bundle):**
  - **Por quê:** qualquer chave embutida num desktop é extraível; pra distribuir com segurança, cada pessoa usa a própria. (Opções discutidas: ofuscar = teatro; proxy server = produção mas exige hospedar; per-usuário = escolhido.)
  - **Servidor (`server/index.mjs`):** `GEMINI_API_KEY` virou `let` (mutável em runtime); `WRITE_ENV_PATH` (data dir no empacotado / `server/.env` em dev) e `persistEnv()` que grava preservando a senha. Endpoints: `GET /api/config` → `{ geminiConfigured }` (nunca devolve a chave); `POST /api/config/gemini` `{ key }` (auth) → valida, grava e atualiza a chave em memória **sem reiniciar**.
  - **Frontend:** estado `geminiConfigured` carregado via `_loadConfig()` (no boot autenticado e após login). Na aba Assistente, se não há chave aparece o card **"Ative o Assistente"** (input + "Salvar chave" + link aistudio.google.com/apikey); com chave, o chat normal. A chave fica só na máquina (data dir), nunca no instalador.
  - **QA (Playwright):** com chave → chat; limpar → card; chave curta rejeitada; colar chave válida → volta pro chat e persiste. Zero erros. (`server/.env` real foi restaurado do backup após os testes — a chave verdadeira está de volta e o Assistente responde.)
  - **⚠️ Pendências do `.exe` (em espera, decididas na hora do build):** (1) o `tauri.conf.json` (linha 32) hoje empacota `server/.env` — pra distribuir, o bundle **não** pode levar a `GEMINI_API_KEY` (nem idealmente um hash de senha compartilhado). (2) **Senha por usuário** (onboarding de 1ª execução) — o usuário pediu pra segurar esse passo. Resolver os dois antes de gerar o instalador final.
- **2026-06-20 — PIVÔ: de `.exe` distribuído para VERSÃO HOSPEDADA MULTIUSUÁRIO.** O usuário decidiu (a) cada um tem o **seu próprio** Centro de Comando (dados separados) e (b) precisa abrir **no celular E no PC de qualquer lugar**. Isso descarta o `.exe` (Windows/local, sem celular remoto) e aponta pra **hospedar uma web app com contas**. Ver memória [[distribuicao-exe-multiusuario]]. **O backend foi refeito pra multiusuário:**
  - **`server/db.mjs`:** nova tabela `users` (id, username único COLLATE NOCASE, password_hash, gemini_key, created_at). `state_store` e `audit_log` agora têm `user_id` (PK composta no state). Migração: se as tabelas antigas existirem sem `user_id`, são recriadas (dados de dev são descartáveis). Funções: `createUser/getUserByUsername/getUserById/setUserGeminiKey/userCount`; `getState/saveState/logAction/updateActionStatus/listActions` agora recebem `userId`.
  - **`server/auth.mjs`:** sessões guardam `{ userId, expiresAt }`; `createSession(userId)` + `getSessionUserId(token)` (substitui `isValidSession`). Hash scrypt e rate-limit por IP iguais.
  - **`server/index.mjs`:** acabou a senha/chave global no `.env` (some `APP_PASSWORD_HASH`, `GEMINI_API_KEY`, `persistEnv`; some a exigência de senha no boot). `authUserId(req,res)` devolve o id da sessão. **Rotas:** `POST /api/signup` (username 3–30 `[a-zA-Z0-9_.-]`, senha ≥6, único; opcional `SIGNUP_CODE` no env pra restringir cadastro), `POST /api/login` (username+senha), `GET /api/me` (devolve `username` + `hasUsers`). `/api/state`, `/api/chat`, `/api/chat/resolve`, `/api/audit`, `/api/config*` todas escopadas pelo `userId` da sessão. **Chave do Gemini agora é por usuário** (coluna `users.gemini_key`), não mais `.env`.
  - **Frontend (`.dc.html`):** tela de acesso virou **login/cadastro** (campo usuário + senha, alterna com "Não tem conta? Criar uma"; instância fresca abre em modo cadastro via `hasUsers:false`). `submitAuth` chama signup ou login. Rodapé da sidebar mostra o **usuário logado** (iniciais + nome) e um botão **Sair** (`/api/logout` + limpa o state local). Ícone `logout` novo.
  - **QA (Playwright, 12 checagens):** instância fresca → cadastro; criar conta entra no app e mostra o usuário na sidebar; cria cliente; Assistente do novo usuário aparece **sem chave** (chave por usuário); logout → login; com usuários o padrão é login; 2º usuário **não vê** os dados do 1º (isolamento ✓, `clients:[]`); re-login do 1º recupera os dados; cadastro duplicado rejeitado. Zero erros. (Curl extra confirmou o isolamento direto na API.)
  - **Obsoletos:** `npm run setup` e o `APP_PASSWORD_HASH`/`GEMINI_API_KEY` no `.env` não são mais usados (contas vivem no DB). **Senha antiga `teste123` não existe mais** — agora cada um cria a própria conta.
  - **Próximo:** hospedar (Render/Railway/Fly) com HTTPS + persistência (volume p/ SQLite ou Postgres) — task #29.
- **2026-06-20 — Deploy preparado pro Fly.io (host escolhido pelo usuário).**
  - **Por que Fly:** o app é servidor Node + SQLite em arquivo; precisa de servidor sempre-ligável + disco persistente (a Vercel é serverless/sem disco → não serve). Fly roda como contêiner com **volume** (o SQLite persiste) e HTTPS.
  - **Arquivos novos:** `Dockerfile` (base `node:24-slim`, **zero `npm install`** — app é zero-deps; `CDC_DATA_DIR=/data`, `PORT=8080`), `fly.toml` (region `gru`, `internal_port 8080`, `force_https`, `[[mounts]] cdc_data→/data`, `auto_stop` + `min_machines_running 0`, vm 256mb), `.dockerignore` (tira `src-tauri` de 3,5 GB, `node_modules`, `.git`, **`server/.env` e `server/data`** — segredos nunca vão pra imagem), `DEPLOY-FLY.md` (passo a passo pro usuário).
  - **Proteção da URL pública:** `SIGNUP_CODE` (env/`fly secrets`) exige código de convite no cadastro; adicionei o campo opcional "Código de convite" no form de cadastro (só no modo signup; enviado como `code`).
  - **Modo hospedado** liga automático quando `CDC_DATA_DIR` existe (`PACKAGED`): serve o `/sw.js` (PWA instalável no celular via HTTPS), sem live-reload, sem abrir navegador. Log de boot agora distingue "modo hospedado/produção" vs "dev".
  - **QA (modo hospedado local, porta 8090, `CDC_DATA_DIR`+`SIGNUP_CODE`):** `/` 200, `/sw.js` 200, `support.js`/`manifest`/`icons` 200, `server/.env` **403** (bloqueado), html **sem** snippet de live-reload, `/api/me` hasUsers:false; signup sem/!código → 403, com código certo passa pro validador de usuário. Tudo ok.
  - **`.gitignore`:** ampliado `server/data/*.sqlite` → `server/data/` (pega `-wal`/`-shm`). Confirmado: nenhum segredo no `git status`.
  - **Falta o usuário fazer (DEPLOY-FLY.md):** criar conta no Fly (pede cartão mesmo no grátis), instalar `flyctl`, `fly launch --no-deploy`, `fly volumes create cdc_data --region gru --size 1`, (opcional) `fly secrets set SIGNUP_CODE=...`, `fly deploy`. Cada pessoa cria conta + põe a própria chave Gemini.
- **2026-06-20 — MUDANÇA DE HOST: o usuário quer 100% grátis (R$0) → escolhido VM "Always Free" do Google Cloud** (não o Fly, que exige cartão e não garante R$0; nem Render, que dorme + dependeria de banco externo). A VM mantém o **SQLite como está** (no disco da VM, 30 GB grátis), fica **sempre ligada** e é grátis pra sempre dentro dos limites.
  - **Arquivos novos:** `deploy/setup-vm.sh` (roda na VM Ubuntu: instala **Node 24** via NodeSource + **Caddy**; cria usuário `cdc`; sobe o app como serviço **systemd** com `CDC_DATA_DIR=$APP/data`, `PORT=8080`, `SIGNUP_CODE` opcional, `Restart=always`; configura **HTTPS automático** via Caddy reverse-proxy + um domínio **DuckDNS** grátis; cron a cada 5 min mantém o DuckDNS apontando pro IP da VM). `DEPLOY-GCLOUD.md` (passo a passo: conta GCP + alerta de orçamento, criar `e2-micro` Ubuntu 24.04 em `us-east1` com HTTP/HTTPS liberados, DuckDNS, mandar o código via **zip pelo SSH do navegador** ou GitHub, rodar o script, usar). `.gitattributes` força **LF** em `*.sh`/`deploy/**` (CRLF do Windows quebraria o script no Linux).
  - **Transferência do código:** sem remote git; app só **616 KB**. Vou gerar um `centro-de-comando-app.zip` (sem `src-tauri`/`node_modules`/`.git`/segredos) pro usuário subir pelo "Upload file" do SSH do navegador. (GitHub público é a alternativa pra updates fáceis — nada de segredo é versionado.)
  - **Persistência:** os dados ficam em `/opt/centro-de-comando/data/cdc.sqlite` no disco da VM; updates (git pull/unzip) não tocam `data/`. Backup: copiar o `.sqlite`.
  - **Pendente do usuário:** criar a VM, DuckDNS, subir o zip, rodar `sudo bash deploy/setup-vm.sh <dominio.duckdns.org> <token> [codigo]`. (Os arquivos do Fly seguem no repo como alternativa, mas o caminho ativo agora é o Google VM.)
- **2026-06-20 — MUDANÇA DE HOST FINAL: Google travou (contas de faturamento fechadas + pré-pagamento de R$ 50 reembolsável). Usuário quer R$0 SEM cartão → escolhido RENDER (grátis) + TURSO (banco libSQL na nuvem, grátis, sem cartão).**
  - **Refatoração do banco (`server/db.mjs`):** trocado `node:sqlite` (síncrono) por **`@libsql/client`** (assíncrono). Mantém o **mesmo dialeto SQLite** (AUTOINCREMENT, `COLLATE NOCASE`, `ON CONFLICT`) — só virou async. `resolveUrl()`: usa `TURSO_DATABASE_URL` (nuvem/Render) se existir, senão arquivo local (`file:` em `CDC_DATA_DIR`/`server/data`). `initDb()` cria as tabelas no boot. **Dropei a migração single→multi antiga** (já passamos dela). Todas as funções viraram `async` e retornam `Number(lastInsertRowid)` (libSQL devolve BigInt).
  - **`server/index.mjs`:** `await` em **todas** as chamadas do banco; `await initDb()` antes do `server.listen` (top-level await, ESM); `PACKAGED` agora também liga com `NODE_ENV==='production'` (Render entra em modo hospedado: serve PWA, sem live-reload/fs.watch/abrir navegador).
  - **`package.json`:** dep `@libsql/client`; script `start`; `engines.node >=20`. **`Dockerfile`** (Fly, alternativo) agora faz `npm install --omit=dev` (não é mais zero-dep) — Fly ainda funciona em file-mode no volume.
  - **Deploy Render:** `render.yaml` (web service free, `npm install` + `node server/index.mjs`, env `NODE_ENV=production` + `TURSO_DATABASE_URL`/`TURSO_AUTH_TOKEN`/`SIGNUP_CODE` como secretos), `DEPLOY-RENDER.md` (3 cadastros grátis: Turso → GitHub → Render).
  - **QA local (libSQL file-mode):** curl — signup/isolamento/persistência ok, sem erros; **Playwright 8/8** — instância fresca→cadastro, criar conta entra, isolamento entre 2 contas (`clients:[]`), relogin recupera dados, duplicado rejeitado, zero erros JS.
  - **Pendente do usuário (DEPLOY-RENDER.md):** criar **Turso** (pegar `libsql://` URL + token), criar repo **GitHub** (público — sem segredos versionados) e dar o link (eu faço o `git push`), criar **Render** web service do repo + colar as 3 envs. Cada um cria conta + põe a própria chave Gemini. **Trade-off:** o app "dorme" quando ocioso (1º acesso ~30-60s).
- **2026-06-21 — Sessão grande de mobile + correções de UX (app já hospedado no Render; cada correção foi commitada no `main` → deploy automático).** Tudo testado a **375px** num "olho mobile" próprio: servidor isolado (`CDC_DATA_DIR` em `%TEMP%`, banco separado) + **puppeteer-core/Edge**, logando um usuário de teste e injetando dados via `PUT /api/state`, navegando pelas telas (a instância do framework DC não é alcançável por fiber → navega clicando nos botões do menu). Ferramenta efêmera; ver memória do Claude `cdc-mobile-preview-harness`.
  - **Mobile — vazamento/deslocamento (raiz):** o conteúdo "ia pra esquerda" e cortava. Causa: cabeçalhos/colunas mais largos que a tela sem nada conter o overflow. Fix (tudo dentro de `@media (max-width:760px)`): `overflow-x:hidden` em html/body/root/`main`; cabeçalhos das telas com `flex-wrap`; grids que estouravam viraram **`minmax(0,1fr)`** (corrige o "grid blowout"). Classes utilitárias novas: `cc-wrap-mob`, `cc-kpi-money`, `cc-grid-funnel`, `cc-navtoggle`.
  - **Reflow por tela (mobile):** Início (futuras/atividades empilham), Metas (1 coluna), Demandas (filtros quebram linha; faixa do cliente numa linha), Financeiro/Salário (números grandes com `nowrap`/fonte menor; donut do salário embaixo), Perfil do cliente (Dados/Demandas em 1 coluna), Clientes (funil/pipeline em **2×2**).
  - **Sidebar (mobile):** abre por **swipe da borda esquerda** (fecha arrastando pra fora — listeners de toque no `componentDidMount`); botão de menu movido pra esquerda (`order:-1`); virou **`position:fixed`** (não rola junto com a página) + fundo travado com a sidebar aberta (`touch-action:none` no backdrop + `overflow:hidden` no body via `componentDidUpdate`).
  - **Calendário:** legenda de clientes virou **filtro clicável** (mostra TODOS, seleciona pra filtrar eventos do mês/semana — state `calClientSel`); **clicar no dia abre um painel do dia** (`calDay`) com as demandas dele + "Nova demanda neste dia" (antes ia direto pra criar).
  - **Workflow:** **toque** completo (arrastar nós/fundo, tocar pra abrir — espelha mouse com `touchstart/move/end`) + **auto-centraliza** no mobile (`_wfFit`); **zoom por pinça** (2 dedos, `kind:'pinch'`); botão **"Organizar"** (`wfAutoArrange`) que reposiciona os blocos e reenquadra.
  - **Lembretes:** desativar **mantém visível** (inativo/esmaecido, toggle reversível `done:!done`) + botão **excluir** à parte; só ativos contam no "X ativos".
  - **Ideias:** botão **salvar desabilitado** (e `return` no `submitDrawer`) enquanto título/observação estiverem vazios.
  - **Demandas:** hierarquia visual (botão "Nova demanda" virou discreto/contorno, filtro de prioridade mais leve, Cliente/Status e abas de cliente com mais peso) — **subjetivo, aguardando OK do usuário**; removido o **botão duplicado** de adicionar demanda (tirado o card tracejado da lane; sobrou um).
  - **Foto do cliente:** já aparecia nos cards; adicionada também na **barra do cliente ativo** (Demandas → por cliente). `clientLanes` ganhou `avatar`/`hasAvatar`/`noAvatar`. (Pendente confirmar se falta em outros lugares: abas por cliente, calendário, cards de demanda.)
  - **Música (desktop/.exe "parou de funcionar"):** o player e o YouTube **funcionam** no teste de navegador (API carrega, iframe criado); `csp:null` no `tauri.conf.json` (não era CSP). Suspeito: o **service worker** cacheava respostas **opacas do YouTube** e podia servir versão quebrada. Fix em `sw.js`: **não intercepta mais YouTube** (`youtube.com`/`youtube-nocookie.com`/`ytimg.com`/`googlevideo.com` vão direto na rede) + cache **`v2`** (a ativação limpa o `v1`). Pra pegar: fechar/abrir o app (baixa o SW novo). **Não reproduzi o bug** — aguardando o usuário confirmar o sintoma exato se persistir.
- **2026-06-21 (continuação) — Foto do cliente espalhada nos lugares que faltavam + hardening do player de música (fios em aberto da sessão anterior; verificado ao vivo).**
  - **Foto do cliente em +3 lugares** (antes só nos cards da lista de clientes e na barra do cliente ativo): (1) **cards de demanda do kanban "Por status"** (`mkCard` + template ~545) — avatar 17px no lugar do dot ao lado do nome do cliente; (2) **abas "Por cliente"** (`clientTabs` + template ~488) — avatar 19px antes do nome; (3) **painel do dia do calendário** (`calDayDemands` + template ~1794) — avatar 30px em cada linha. **Helper compartilhado novo `_clAv(cl)`** (`avatar`/`hasAvatar`/`noAvatar`/`avatarBg`/`avInitials`/`avColor`), definido antes de `mkCard` e reusado nos 3. **Fallback gracioso:** sem foto, mantém o dot/inicial atual — quem não cadastrou foto não vê mudança nenhuma (**zero regressão**).
  - **Player de música — hardening (provável causa real do "parou no .exe", além do fix de SW da sessão anterior):** `_ytEnsure` agora passa **`origin` explícito** (`window.location.origin`, só se `http(s)`) + `enablejsapi:1` nos `playerVars`. Sem o `origin`, o embed do YouTube pode **ignorar os comandos** dentro de webview embutido (Tauri/WebView2) por mismatch no handshake de origem do `postMessage` — sintoma idêntico ao relatado ("toca no navegador, não toca embutido"). Corrigido também um **bug real**: `_ytEnsure` chamado 2× antes do `onReady` criava **dois `YT.Player` no mesmo `#cdc-yt`** — agora os callbacks são enfileirados (`_ytReadyCbs`) com flag `_ytCreating`. E **pré-aquecimento**: abrir o painel de música (`toggleMusic`) já instancia o player, pra o 1º play rodar dentro do gesto do clique (resolve o clássico "1º clique não toca").
  - **Demandas — hierarquia visual** (botão "Nova demanda" discreto, filtro mais leve, abas com peso): tratada como **aprovada** (usuário pediu "pode fazer tudo de uma vez"; sem pedido de reverter).
  - **Verificação ao vivo** (puppeteer-core dirigindo o Chrome, servidor isolado em `CDC_DATA_DIR`/`%TEMP%` na porta 5199, banco vazio + 2 clientes (1 com foto, 1 sem) + 3 demandas): kanban "Por status" → **2 avatares** (as 2 demandas do Acme) + dot na demanda do cliente sem foto; "Por cliente" → avatar na aba do Acme + na barra ativa + dot na aba sem-foto (**2 imgs**); painel do dia 21 → avatar do Acme + **iniciais "SF"** do cliente sem foto (**1 img**); **música: `#cdc-yt` único (sem dupla criação) + iframe com `enablejsapi=1` e `origin=http://localhost:5199` presentes** (fix do handshake confirmado). Screenshots conferidos. Sintaxe do bloco `data-dc-script` validada via `node --check`. Os únicos "erros" de console são **9 404 de placeholder do framework DC** (ele renderiza `<img src="{{ x.avatar }}">` literal no skeleton inicial, 404a e some ao bindar) — **6 já existiam** (`userAvatar`/`cl.avatar`/`activeClientInfo.avatar`/`clienteProfile.avatar`/`clienteAvatarPreview`/`profileAvatarPreview`) + 3 novos do mesmo padrão; benignos. Temporários, `puppeteer-core` (`--no-save`), data dir e screenshots removidos — working tree só com o `.dc.html` modificado.
- **2026-06-21 (continuação 2) — Bug "clico na música e não aparece nada" CORRIGIDO (painel abria fora da tela).** Usuário reportou; reproduzido e confirmado.
  - **Causa raiz:** o painel do player é arrastável e a posição (`musicPos`) é salva no `localStorage` (`cdc-audio`). Se foi salva numa janela maior / outro monitor / resolução diferente, ao reabrir numa janela menor o painel renderizava **fora do viewport** (ex.: `left:5000` num viewport de 1440px). O painel **existia no DOM** (botões Minimizar/Fechar presentes, player criado) mas longe da área visível → "não aparece nada". `musicLeft`/`musicTop` liam a posição salva **sem reclampar** pro viewport atual; o clamp só existia durante o arrasto. (Confirmado também que não é mobile — o `.cc-hide-mob` e o `isMobile` usam o mesmo breakpoint 760px, consistentes; quem clica está no desktop.)
  - **Fix:** novo helper **`_musicClampPos(p)`** que limita x/y ao viewport atual (margem 8px, painel 280×~372). Aplicado em **2 pontos**: na **abertura** (`toggleMusic` agora faz `ns.musicPos = this._musicClampPos(s.musicPos)`, sanando a posição salva toda vez que abre) e na **renderização** (`musicLeft`/`musicTop` clampam — defesa contra redimensionar a janela com o painel já aberto).
  - **Verificação (puppeteer/Chrome, 4 cenários, auth idempotente signup-ou-login):** posições salvas off-screen `x:5000`, `x:-400`, `y:3000` — **antes** abriam fora da tela (`onScreen:false`); **depois** os 3 (+ o default) abrem **totalmente visíveis** (`fullyVisible:true`, grudam na borda mais próxima). Screenshot confere o painel reaparecendo na borda direita com `x:5000` salvo. Sintaxe `node --check` OK. **Pegadinha do teste** (documentada pra não repetir): re-rodar um script que faz só `signup` dá falso-negativo (usuário já existe → não loga → sem botão de música) — por isso a auth virou signup-ou-login.
- **2026-06-21 (continuação 3) — Player de música: 3 problemas reportados pelo usuário, todos corrigidos e verificados.**
  - **(1) "As músicas não tocam" — CAUSA RAIZ: IDs de estação mortos/bloqueados (NÃO era o código).** Diagnóstico via puppeteer testando a embedabilidade de cada ID num player YT real: a estação **padrão** (índice 0, `jfKfPfyJRdk` "Lofi estudar"), "Lofi relaxar" (`28KRPhVzCus`) e "Cachoeira" (`BHntJ_zlYCQ`) davam **erro 150** (embed bloqueado) — lives de lofi **rotacionam de ID** quando reiniciam e o antigo vira não-embedável. Como o app abre tocando a estação padrão (morta), "não tocava nada". **Fix:** `_lofiCats` reescrito com **8 IDs verificados (embed OK + chegando a PLAYING)**, títulos confirmados via **oEmbed do YouTube**, **calmos primeiro** (índice 0 = padrão agora é Chillhop lofi 24/7 que toca): Lofi estudar (`7NOSDKb0HlU`), Lofi jazzy (`5yx6BWlEVcY`), Lofi dormir (`qH3fETPsqXU`), Lofi sessão calma (`lTRiuFIWV54`), Dark ambient (`S_MOd40zlYU`), Synthwave (`4xDzrJKXOOY`), Chuva com trovão (`mPZkdNFkNps`), Lareira (`L_LUpnjgPso`). Comentário do `_lofiCats` atualizado avisando da fragilidade dos IDs de live (re-testar embed se algum parar).
  - **(2) "Não dá pra mexer quando encolhido" — bug que EU tinha introduzido na continuação 2.** O clamp de **render** (`musicLeft`/`musicTop` via `_musicClampPos`, com as dims do painel **expandido** 280×372) **brigava com o arrasto**: o painel encolhido (~46px de altura) era clampado como se tivesse 372px, então não descia além de `y=h-372` (528px num viewport de 900) nem passava de `x=w-288` — "puxava de volta". Reproduzido (arrastar pra (1350,820) parava em (1152,528)). **Fix:** **removido o clamp de render** (volta a usar a posição crua, como era antes); **mantido só o clamp na abertura** (`toggleMusic`), que já resolve o "abre fora da tela" da continuação 2. Os dois coexistem: arrasto livre + abrir sempre on-screen (o próprio `_musicMoveB` já auto-limita o arrasto a `w-60`/`h-50`, mantendo o painel sempre agarrável).
  - **(3) "Mais rádios calmas / lofis mais calmos" — atendido:** 8 estações (eram 6), **5 lofis calmos** no topo (estudar/jazzy/dormir/sessão calma/dark ambient).
  - **Verificação (puppeteer/Chrome):** embedabilidade dos 18 candidatos (8 tocam, 10 com erro 150); títulos reais via oEmbed; **playback no app real** = label "Tocando" (autoritativo — vem do `onStateChange=playing` do próprio player) com a estação padrão nova; **arrasto encolhido livre** (alvo 1350/820 → 1325/797) **sem perder o clamp de abertura** (off-screen `x:5000`/`x:-400`/`y:3000` → `fullyVisible`). Screenshot confere player tocando + seletor com as 8 estações. Sintaxe `node --check` OK. Temporários/`puppeteer-core`/data dir/screenshots removidos.
- **2026-06-22 — Lote de 8 ajustes pedidos pelo usuário (rápido em sequência), todos implementados e verificados (puppeteer/Chrome, 2 rodadas + screenshots).**
  - **(1) Sino — ponto vermelho só com avisos NOVOS.** Antes `hasAvisos` (qualquer aviso) acendia o ponto, então ficava sempre vermelho. Agora separados: `hasAvisos` (tem algum → conteúdo do painel) e **`hasUnseenAvisos`** (não vistos → ponto vermelho). Estado `notifSeen` (persistido em `cdc-notif-seen`, IIFE no state inicial); `toggleNotif` ao **abrir** marca todos os avisos atuais como vistos (e poda: só ficam as chaves atuais → um aviso que some e volta re-alerta). Verificado: ponto true antes de abrir, painel mostra avisos, ponto false depois.
  - **(2) Botão "Nova demanda" duplicado na visão "Por cliente"** — removido o da barra do cliente ativo (mantido o do cabeçalho). Verificado: 1 botão visível.
  - **(3) Volume do "toque" (efeitos de clique).** Antes só `soundOn` (liga/desliga). Agora `sfxVol` (0-100, default 70, persistido em `cdc-audio`), o `_sfx` escala o `peak` por `sfxVol/100` (e `return` se vol 0). **Popover** novo a partir do ícone de som no titlebar (`sfxPanelOpen`): liga/desliga + slider de volume (toca um clique de preview ao arrastar).
  - **(4) Cropper circular da foto (cliente E perfil).** Antes o `_imgToDataUrl` fazia center-crop automático (cortava errado se o sujeito não estava centrado). Agora `clienteAvatarChange`/`profileAvatarChange` abrem um **modal de recorte circular** (`_openCropper`): círculo de 260px com a imagem arrastável (pan, mouse+touch) + **slider de zoom** (1×–3×) + máscara escura ao redor + anel accent. `_applyCrop` rende o quadrado 160px (canvas `drawImage` com source-rect do que está no círculo) → data URL → seta o avatar. Helpers: `_cropGeom` (cover+zoom+pan clampado), `_cropDragDown`. `cropTarget` distingue cliente/perfil. Verificado: abre com imagem+zoom, Aplicar fecha e seta o avatar.
  - **(5) Tema: branco mais quente + preto mais claro + slider de claridade.** Tokens `:root[data-theme]` ajustados — **dark**: blacks elevados (bg `#0d0e10`→`#17181c`, app-bg `#060708`→`#101114`, ladder toda lift ~+9; gradiente idem). **light**: off-white quente (bg `#eaecef`→`#efebe4`, surface `#fff`→`#fdfbf6`, app-bg `#e4e7eb`→`#e9e3d9`, bordas/texto levemente quentes). **Slider de claridade** num **popover de Aparência** (do ícone de tema): segmentado Escuro/Claro + slider `bright` (-100..100, persistido em `cdc-bright`) que controla um **overlay `mix-blend-mode:soft-light`** (div fixed `pointer-events:none` z-index máximo — **NÃO usa `filter` na raiz** pra não quebrar `position:fixed`; cor branco/preto + opacidade `|bright|/100*0.5`). `_applyTheme(mode)` extraído (morph) e reusado por toggle + popover. Verificado: overlay 0→0.4 com bright=80; Claro → `data-theme=light` + bg `rgb(233,227,217)`.
  - **(6) Perfil do cliente** — "Excluir cliente" virou botão **destacado vermelho no topo** (linha do "Voltar", à direita); removidos o "Nova demanda" grande do cabeçalho e o X de excluir. Mantidos "Editar", o toggle de pago, e a **"Nova" pequena** da seção de demandas (que **pré-preenche o cliente** — `onNovaDemanda` já fazia `drawerDemandClientId: _cp.id`). Verificado: Excluir cliente=true, Nova demanda grande=0, Nova pequena=true.
  - **(7) Auditoria dos botões "Nova demanda"** (usuário: "não deixe pelo site inteiro"). **Removidos** (redundantes): "+" do cabeçalho de cada coluna do kanban, card dashed "Novo projeto pessoal" (Pessoal). **Mantidos** (fazem sentido): Início (CTA), Demandas header, Calendário header, "Adicionar" por coluna do kanban, "Novo projeto" (Pessoal), empty-state "Criar demanda", "Nova" do perfil, "Virar projeto" (ideias).
  - **(8) Pré-preencher cliente no contexto.** Novo `newDemandaBoard` no botão "Nova demanda" do cabeçalho de Demandas: na visão **"Por cliente"** já entra com o cliente da **aba ativa** selecionado (`drawerDemandClientId`). O perfil do cliente já pré-preenchia. Verificado: clicar na visão Por cliente abre o drawer com "Acme" selecionado.
  - **Verificação:** sintaxe `node --check` OK em cada etapa; 2 rodadas puppeteer (titlebar: sino/som/tema/claridade; depois cropper/auditoria/prefill/perfil) + screenshots do cropper, dark e light conferidos. **Zero erros de console** (fora os 404 de placeholder já conhecidos). Pegadinha de teste (de novo): username de signup precisa ter ≥3 chars (`v1` falhava com 400 → caía no login). Temporários/puppeteer/dados/screenshots removidos.
- **2026-06-22 (cont.) — Música: playlist de estações + nome real; tonalizador (quadradinhos) no lugar do brilho; player sobe ao expandir.**
  - **(9) Playlist de estações YouTube + nome real.** Antes só 1 link custom ("Link personalizado"). Agora `customStations: [{id,name}]` (persistido em `cdc-audio.customs`, com migração do `custom` antigo). `_musicCustom` **adiciona à playlist** e toca; `_musicPlayCustom`/`_musicRemoveCustom`. **Nome real** via `_ytSyncTitle` → `this._yt.getVideoData().title` no `onStateChange` (sem CORS/serviço externo; resolve ao tocar e grava no nome da estação salva). Picker reorganizado: seção **"Minhas estações"** (nome + ×), "Categorias", "Adicionar link do YouTube". `musicStationName` mostra o nome resolvido. Verificado: 2 links salvos viram "1 A.M Study Session…" e "Chillhop Radio - jazzy…" no localStorage e na tela.
  - **(10) Tonalizador (substitui o slider de brilho).** Usuário: "não brilho, e sim tonalidade — branco↔creme / preto↔cinza", e "quadrados de seleção de cores". Removido o overlay `mix-blend`/`bright`. Agora **5 swatches** no popover de Aparência: por tema, interpola os tokens de fundo entre 2 endpoints (`_toneEndpoints` light=branco↔creme, dark=preto↔cinza) via `_lerpHex`/`_toneTokens`, aplicados com `documentElement.style.setProperty` (`_applyTone` — **não usa `filter`**, sem quebrar `position:fixed`). Estado `toneLight`/`toneDark` (default 50, persistido em `cdc-tone` por tema); `_setTone` aplica na hora; `_applyTheme` reaplica ao trocar tema; `_applyTone` no mount. Verificado: clicar swatch muda `--app-bg` (#0f1013→#17191e) e salva `{dark:100}`.
  - **(11) Player sobe ao expandir.** `toggleMusicMin`, ao **desencolher**, reclampa `musicPos` com `_musicClampPos` (altura expandida 372px) → se estava embaixo, sobe pra caber. 1 linha, reusa lógica já verificada.
  - **Verificação:** `node --check` OK; puppeteer (tonalizador: 5 swatches + muda `--app-bg` + persiste; playlist: 2 links salvos com nomes reais resolvidos) + screenshots conferidos. Zero erros. Pegadinha de teste: username signup ≥3 chars.
  - **Pendências do usuário (próximo lote):** (12) **drag-and-drop de demandas no mobile/toque** — o DnD atual é HTML5 (não funciona em toque); reescrever com touch (arrastar card no kanban muda status; arrastar pra outra aba de cliente reatribui). NÃO mexer na música no mobile (ver memória `mobile-experiencia-diferente`). (13) **.exe instância única** — abrir 2ª vez deve focar a janela aberta (plugin `tauri-plugin-single-instance` no `src-tauri` + rebuild); **confirmar se o usuário usa o .exe ou a web** (no PWA o navegador já cuida disso).
- **2026-06-22 (cont.) — Versão do app + tela de Novidades; barra de tempo da música (seek) + next/prev na playlist. (+ rebuild do .exe 0.2.0 com instância única, instalado e testado.)**
  - **(15) Versão + Novidades.** Constante `_appVersion = '0.2.0'` + array `_changelog` (adicionar entrada no topo a cada atualização). Rodapé da sidebar mostra **"v0.2.0 · novidades"** clicável (abre um modal com o changelog) + **ponto azul** quando há versão não vista (`seenVersion` em `cdc-seen-version`; abrir marca como vista). Modal "O que há de novo" lista as releases (v + data + bullets).
  - **(16) Barra de tempo (seek tipo YouTube) + next/prev na playlist.** Player ganhou uma **seek bar arrastável** (`getCurrentTime`/`getDuration`/`seekTo`), com labels de tempo (`_fmtTime`), timer de progresso (`_ytStartProgress`, 1s, só re-renderiza quando o painel está visível e o tempo muda >0.4s) e `musicSeeking` pra não brigar com o arrasto. Aparece quando `musicDuration > 1` (vídeos com duração; live aparece com a janela seekável). **Next/prev** (`_musicCycle`) agora cicla **dentro da playlist custom** quando uma estação custom toca (senão entre as categorias); **auto-advance** ao terminar um vídeo (`onStateChange` data 0). Verificado: seek bar com duração real (3673s/61:13) + posição; modal de novidades com 9 itens + marca versão vista; zero erros.
  - **.exe 0.2.0:** rebuild com `tauri-plugin-single-instance` (compila), versão bumpada 0.1.0→0.2.0 (`tauri.conf.json`+`Cargo.toml`). Instalador gerado, **antigo desinstalado + apagado, novo instalado** (só 1 no registro), e **single-instance testado ao vivo** (abrir 2× = 1 processo só). O `.exe` carrega a URL hospedada, então já vem com todas as features web.
- **2026-06-22 (cont.) — #12 Drag-and-drop de demandas por TOQUE (mobile).** O DnD existente era HTML5 (`draggable`/`onDrop`), que não dispara em touch. Adicionado `_demandTouchStart(e, id)`: **long-press** (200ms) pega o card (evita conflito com o scroll; se mover >8px antes, cancela = scroll), cria um **ghost** que segue o dedo, e usa `document.elementFromPoint` no `touchmove` pra achar o alvo sob o dedo — `[data-dropstatus]` (coluna do kanban → muda `status`) ou `[data-dropclient]` (aba de cliente → reatribui `clientId`; `avul` = sem cliente). `touchend` aplica e dá `preventDefault` pra não disparar o "click" que abriria a edição. Funciona genérico nas duas views (só o alvo visível na view atual é detectado). Atributos `data-dropstatus`/`data-dropclient` (+ `dropKey` nas abas) e `onTouchStart` nos cards (kanban e por-cliente, só demandas reais/editáveis). HTML5 DnD do desktop intacto. **Verificado (puppeteer, viewport 720px touch):** arrastar todo→prod muda status pra `prod`; arrastar pra aba Beta muda `clientId` pra 2. Zero erros. (Nota: o kanban no mobile tem scroll horizontal de colunas 300px — arrastar entre colunas funciona quando ambas estão visíveis; "Por cliente" as abas ficam sempre no topo, mais fácil.)
- **2026-06-22 (cont.) — #14 Mapa mental — Passo 1 (unificar a casca) ENTREGUE.** Baseado no relatório do Obsidian (estudo multi-agente). Abas **Workflow + Ideias unificadas** numa só: **"Mapa mental"** na sidebar (saíram os 2 itens `ideias`/`workflow`, entrou `mapa`). A tela tem um **toggle Cérebro / Ideias** (`mapaMode` 'graph'|'ideias', default graph): **Cérebro** = o canvas-grafo radial que já existia (reusado 100% — pan/zoom/toque, nós coloridos por status/categoria, "Organizar"); **Ideias** = a lista/grid de ideias. Header compartilhado (título + toggle + ações por modo: "Organizar" no Cérebro, "Nova ideia" nas Ideias). Rewiring: `go('mapa')` chama `_wfFit`; navegações pra `screen:'ideias'` viraram `screen:'mapa', mapaMode:'ideias'` (inclusive o nó "Ideias" do grafo); `isBuilt` ganhou 'mapa'. Cirurgia de template (encapsular os 2 sc-ifs num wrapper com 2 sub-modos) — balanceei as tags com cuidado (o `node --check` não pega HTML). **Verificado (puppeteer):** sidebar só com "Mapa mental"; Cérebro renderiza canvas+nós+Organizar; trocar pra Ideias mostra o card da ideia + "Nova ideia" e o canvas some; zero erros. Screenshot confere.
  - **PRÓXIMOS PASSOS do #14 (do relatório, ainda não feitos):** Passo 2 — layout **force-directed** opcional + nó ∝ nº de conexões (cor por categoria/status já existe parcialmente); Passo 3 — **modo Blueprint** (canvas editável: criar cards livres + conectar nós manualmente, persistir JSON no state); Passo 4 — painel "Conexões desta nota" (backlinks light) + "Virar projeto" pelo nó. O modo Blueprint é o maior (desktop-first).
- **2026-06-22 (cont.) — #14 Mapa mental — melhorias do Cérebro (graph): cor+legenda (P3) e force-directed+hubs (P2).** Usuário escolheu adicionar tudo do menu (cor/legenda, force-directed, blueprint, filtro/ERP); estes 2 primeiros entregues:
  - **Cor + legenda:** ideias agora coloridas por **categoria** (catColors: Pessoal/Produto/Conteúdo/Cliente) em vez de tudo âmbar; demandas já eram por status. **Legenda** nova no canto inferior-esquerdo do canvas (Demandas: A fazer/Produção/Revisão/Entregue; Ideias: 4 categorias), escondida no mobile (`cc-hide-mob`).
  - **Force-directed + hubs:** nó de cliente agora tem **tamanho proporcional ao nº de demandas** (`size = 46 + min(items,9)*3.4`). Botão **"Espalhar"** (novo) roda `_wfForce()` — simulação física (repulsão O(n²) entre todos os nós + molas nas arestas, 300 iterações, root preso no centro, normaliza pra caber 1120×800) e salva as posições em `wfPos` (reusa o mecanismo de override + drag existente). Botão **"Radial"** (era "Organizar") volta ao layout radial. A estrutura do grafo (ids+arestas+base) é stashada em `this._wfSim` no render pro `_wfForce` usar. Verificado (puppeteer + screenshot): legenda presente, cliente "Grande" (6 dem) maior que "Pequeno" (1), Espalhar muda as posições, zero erros.
  - **Falta do #14:** filtro/foco local + painel de conexões + "virar projeto" pelo nó (P3,4,7,8); e o **Blueprint editável** (P5,6 — o maior, desktop-first).
- **2026-06-22 (cont.) — #14 Mapa mental — foco local + painel de conexões + "virar projeto" (P4, P7, P8).** Clicar num nó do Cérebro agora **foca nele** em vez de navegar direto: o nó + vizinhos diretos ficam acesos e **todo o resto escurece** (`nodeOpacity` .14, links não-conectados op .05) — o "local graph" do Obsidian. Abre um **painel de Conexões** (card flutuante no canto sup-direito do canvas): tipo do nó (Agência/Cliente/Demanda/Ideia, derivado do id via `wfTypeOf`) + nome + **lista de conexões clicáveis** (navega o foco) + botão **"Abrir"** (faz a navegação original — `wfNav[id]`) + **"Virar projeto"** quando é uma ideia (`projetoFromIdeia`, parseando o idx do id `l:ideias:i<n>`) + X "Limpar foco". Implementação: maps `wfNav`/`wfMeta` montados no `wfPush`, arestas ganharam ids (`u`/`v`) no `wfLink` pra dimming, pós-processamento no render monta `wfFocusPanel` a partir de `s.wfFocus`. Verificado (puppeteer + screenshot): clicar cliente → painel com 3 conexões + Abrir + 4 nós escurecidos; clicar ideia → botão "Virar projeto". Zero erros de app.
  - **Falta do #14:** filtro do grafo (P3 — por cliente/categoria/status; pode virar legenda clicável) e o **Blueprint editável** (P5/P6 — sessão própria, o maior).
- **2026-06-22 (cont.) — #14 filtro do grafo (P3) + ajustes de música + drag de demandas no .exe.**
  - **#14 Filtro (P3) — fecha o pacote do Cérebro:** a **legenda virou clicável** (estilo grupos do Obsidian). Clicar num status (A fazer/Produção/Revisão/Entregue) ou categoria (Pessoal/Produto/Conteúdo/Cliente) **filtra o grafo** — escurece as folhas que não batem (`wfFilter` 'st:<status>'/'cat:<cat>'); clicar de novo limpa. As demandas/ideias ganharam `fk` (chave de filtro), threadada via `wfPush`/`wfMeta`. Combina com o foco (dimming multiplicado). O item ativo da legenda fica destacado. **Com isso o #14 tem: Passo 1 (unificar) + cor/legenda + force-directed/hubs + foco/conexões/virar + filtro. Falta só o Blueprint (sessão própria).** Verificado (puppeteer): clicar "Produção" escurece 2 nós não-prod; clicar de novo limpa.
  - **Música:** (A) **barra de tempo só nos links do YouTube** que o usuário adiciona, não nas lofi (`musicHasDuration` agora exige `musicCustom`) — as lofi são lives 24/7, seek não faz sentido. (B) seção de playlist **sempre visível** ("Minha playlist", com estado vazio convidando a colar um link). (C) o **X agora pausa a música** (`closeMusic` chama `pauseVideo`), não só esconde o painel. (D) o botão de **minimizar** ficou mais claro (ícone gira 180° + tooltip "Expandir"/"Minimizar").
  - **Drag de demandas no .exe (WebView2):** o HTML5 drag-and-drop pode ser engolido pelo handler de file-drop do Tauri. **Unifiquei o drag em MOUSE + TOQUE** (custom, sem HTML5): `_demandTouchStart` agora detecta mouse vs toque — mouse arrasta ao mover (sem long-press), toque mantém o long-press; ghost + `elementFromPoint` iguais. Cards viraram `draggable="false"` + `onMouseDown`/`onTouchStart` (saiu o `onDragStart`/`onDragEnd` HTML5). Funciona no browser, no mobile E no .exe. Verificado (puppeteer mouse): arrastar todo→prod muda o status. (As colunas/abas mantêm `data-dropstatus`/`data-dropclient`.)
  - **Pendente:** #17 (.exe em janela menor não reflui — parece maximizado); Blueprint do #14 (sessão própria).
- **2026-06-22 (cont.) — #17 .exe janela menor: conteúdo não reflowava (root via `100vw`/`100vh`).** Usuário: no .exe, a janela encolhe mas o conteúdo continua tamanho de tela cheia. Diagnóstico: no **browser** o resize ao vivo funciona (root acompanha o viewport), então é específico do **WebView2/Tauri frameless** — o `width:100vw; height:100vh` do root pode não atualizar no resize do webview. **Fix:** root (`[data-theme-root]`) trocado de `width:100vw; height:100vh` por **`position:fixed; top:0; left:0; right:0; bottom:0`** — fixa direto nas bordas do viewport (immune ao cache de unidades vw/vh do webview) e sobrevive aos re-renders (está no template). Verificado (puppeteer, resize AO VIVO sem reload): rootW/rootH acompanham 1400→1000→860, sem overflow-x, sidebar visível, layout reflowa (screenshot a 860px ok). **Ressalva:** como o bug é específico do webview e não reproduz no browser, é um fix de melhor-esforço — se no .exe ainda não refluir, o problema é mais fundo (webview não redimensionando com a janela no Tauri) e aí precisaria de mudança no Tauri + rebuild. Como o .exe carrega a web, esse fix entra sem rebuild.
- **2026-06-22 (cont.) — #14 Mapa mental: BLUEPRINT (canvas editável) — último passo.** Implementado o modo Blueprint (P5+P6 do estudo Obsidian): terceiro modo no toggle do Mapa mental (Cérebro / **Blueprint** / Ideias). Whiteboard livre com pan (arrastar fundo) + zoom (roda/​botões/centralizar). **Cards** de ideia: criar ("Adicionar card"), editar texto (textarea), arrastar (mouse+toque), pintar (paleta de 6 cores quando selecionado), excluir (×). **Conexões manuais**: clicar 🔗 num card → clicar 🔗 em outro → seta entre eles (linha rotacionada); banner "Ligando…" no header + cancelar; excluir aresta (× no hover do meio da linha). **Persistência**: `bpNodes`/`bpEdges` no state → `/api/state` (debounce 400ms). Precisou somar `bpNodes`/`bpEdges` em DOIS lugares: `_persistKeys` (cliente, .dc.html) **e** `STATE_KEYS` (servidor, `server/db.mjs`) — o servidor filtra por whitelist tanto no save quanto no load, então sem isso o blueprint não salvava (descoberto no teste). Estado novo: bpNodes/bpEdges/bpPan/bpZoom/bpConnectFrom/bpEditId; métodos `_bpAddNode/_bpSetText/_bpSetColor/_bpDelNode/_bpDelEdge/_bpConnect/_bpNodeDown/_bpCanvasDown/_bpZoom/_bpWheel/_bpResetView`. Ícone "ligar" reusa `this.icon('workflow')`. Verificado (puppeteer, server isolado): criar 2 cards, digitar, conectar (1 aresta), arrastar (686→806px), persistir no servidor (bpNodes:3/bpEdges:1) e **sobreviver ao reload** (hidratação ok). Versão in-app bumpada 0.2.0→**0.3.0** + entrada na tela de Novidades. NÃO precisa rebuild do .exe (carrega a web); o server muda no deploy do Render.
- **2026-06-22 (cont.) — Blueprint: cards REDIMENSIONÁVEIS + endurecimento por revisão adversarial.** Pedido: "melhore os cards de escrita no blueprint, que sejam alteráveis os tamanhos". Cards ganharam `w`/`h` (default 184×112) com **alça de resize** no canto inferior-direito (mouse+toque, divide pelo zoom). Card virou `flex-column` com `height` explícito: textarea `flex:1` preenche e rola; paleta de cor vira footer. Âncora das arestas passou de header→**centro** de cada card. Constantes `_bpDef/_bpMin/_bpMax W/H`. **Verificado no browser** (puppeteer): grow 184×112→324×202, shrink clampa em 150×86, sem seleção de texto, persiste e sobrevive reload, aresta acompanha. **Depois rodei uma revisão adversarial multi-agente (Workflow, 4 lentes)** que achou 3 bloqueadores reais — todos corrigidos e re-verificados: (1) **aresta apontava pro centro escondido** → agora **recortada na borda** do retângulo (interseção); fio só no vão visível; some se cards se sobrepõem; (2) **sem teto de tamanho** (card monstro persistido) → clamp **máx 720×560**; (3) **z-index:2 fixo** deixava a alça inalcançável sob card sobreposto → **z dinâmico** (`bpFront`/`bpEditId` → z5), card ativo vem pra frente. Também: **vazamento de `userSelect`** se o drag fosse interrompido → helper `_bpBindDrag` com listeners de `blur`+`touchcancel`; **paleta estourava** na largura mínima → gap/padding menores + `flex-shrink:0`; **min height 86→96** (textarea menos espremido ao editar); alça maior (26px) com cor accent + hover (affordance). NÃO precisa rebuild do .exe (carrega a web).
- **2026-06-22 (cont.) — Sincronização celular↔.exe (bug reportado) + 3 ajustes de demandas. Versão in-app 0.3.0→0.4.0.** O usuário reportou: "criei demandas no celular e não apareceu no executável" (mesma conta, confirmado). **Diagnóstico:** o `src-tauri/src/lib.rs` carrega a URL hospedada (`onrender.com`) — então a doc da Fase 3d (sidecar Node local) está OBSOLETA; celular e .exe leem do mesmo Turso. A causa real era o **frontend só buscar o estado uma vez** (`_loadState` no boot/login, sem polling) somado ao .exe **esconder na bandeja sem recarregar o webview** (`WindowEvent::CloseRequested` → `window.hide()` + `prevent_close`): dado criado no celular nunca reaparecia até sair de vez e reabrir. O service worker foi descartado como causa (network-first, nunca intercepta `/api/*`).
  - **Fix de sync (`Centro de Comando.dc.html`):** `_refreshState()` novo, chamado nos listeners `window 'focus'` + `document 'visibilitychange'` (registrados no `componentDidMount`, removidos no unmount). Re-busca `/api/state` ao voltar o foco. Throttle de 2,5s (`_lastStateLoad`, setado em `_loadState`) evita rajada de eventos. Protege gravação local pendente: se `_saveTimer` está ativo, dá flush ANTES de re-buscar. **Bug que EU introduzi e o teste pegou:** o `setState` override não zerava `_saveTimer` depois que o `setTimeout` disparava → `_refreshState` achava que havia save pendente e dava **flush do estado VELHO por cima do servidor** (clobber da mudança remota). Corrigido pondo `this._saveTimer = null` dentro do callback do timeout. Beneficia também o PWA do celular.
  - **(prioridade) Demandas ordenadas por prioridade** alta→média→baixa (desempate = mais recente, id desc). `prioRank`/`sortPrio` aplicados em: kanban por status, lanes por cliente, avulsas, pessoal e lista do perfil do cliente.
  - **(escopo) Seletor Profissional × Pessoal** no header de Demandas (estado `demandScope`, default `prof`). `passScope(d)`: prof = tem cliente válido; pessoal = sem cliente. Filtra o board "Por status" (contagem+cards) e troca o "Por cliente": prof = uma lane/aba por cliente (some a antiga "Avulsas"); pessoal = uma única lane/aba "Pessoal" com as demandas sem cliente. `scopeProf/scopePessoal` resetam `activeClient:0`. `newDemandaBoard` só pré-preenche cliente quando `isProf`. Atende "não quero que as pessoais se misturem com as profissionais".
  - **(status) Seletor de status no editor da demanda** (A fazer/Produção/Revisão/Entregue), espelhando o seletor de prioridade. Estado `drawerStatus`, inicializado em todos os pontos que abrem o drawer (editDemanda=`d.status`; create=`todo`); `submitDrawer` grava `status` no create E no edit. Atende "mudar de a fazer pra entregue sem arrastar".
  - **Verificação (puppeteer/Chrome headless, server isolado em modo produção/file-mode, porta 5191, seed de 5 demandas):** ordenação alta<média<baixa no board ✓; escopo prof exclui pessoal / pessoal exclui clientes ✓; seletor de status presente no drawer, clicar "Entregue" + Salvar → `/api/state` confirma `status:done` (autoritativo) ✓; **sync:** demanda criada via API ("outro aparelho") NÃO aparece sozinha, mas aparece ao disparar o evento de foco ✓ (e a escrita externa não é mais clobberada). Screenshot conferido. Sintaxe `node --check` ok. Temporários/puppeteer-core(`--no-save`)/data dir removidos. **NÃO precisa rebuild do .exe** (carrega a web); vai pro ar no deploy do Render.
- **2026-06-22 (cont.) — #17 REVISITADO: conteúdo do .exe não reflui ao encolher a janela (Trilha A, web). Versão 0.4.0→0.4.1.** Usuário reportou de novo que o fix anterior (root `100vw/100vh`→`position:fixed;inset:0`, commit 5968b86) NÃO resolveu no .exe. **Investigação multi-agente (Workflow, 5 agentes: auditoria CSS + auditoria Rust/Tauri + 2 pesquisas web + síntese).** **Causa raiz (alta confiança, NÃO é CSS):** o *surface*/viewport do WebView2 fica **stale** ao redimensionar a janela FRAMELESS via `startResizeDragging` (loop modal SC_SIZE do Windows) — o controller do WebView2 não reaplica `Bounds` de forma confiável; aí `position:fixed;inset:0` e `100vw/vh` resolvem contra o tamanho ANTIGO e o conteúdo não encolhe. No navegador funciona (viewport = a própria aba), por isso só reproduz no .exe. Agravante: NÃO existia nenhum `addEventListener('resize')`/`ResizeObserver`/`onResized` no app (só `startResizeDragging`), então nada forçava relayout. Refs: tauri#6322/#6927/#9053, WebView2Feedback#2776/#4538, tauri#13318/#9611.
  - **Fix Trilha A (só web, SEM rebuild — entra pelo deploy e já vale no .exe que carrega a web):** em `componentDidMount` (após o bloco do `matchMedia`), mede o tamanho REAL (`visualViewport`/`clientWidth`) e trava **width/height EXPLÍCITOS em px** no `[data-theme-root]` (em vez de depender do viewport stale), reagindo a `ResizeObserver(documentElement)` + `window 'resize'` + `visualViewport 'resize'` + `onResized` do Tauri, com coalescing via `requestAnimationFrame` (`_applyRealSize`/`_scheduleSize`/`_sizeRO`/`_sizeRaf`; também seta CSS vars `--vw`/`--vh`). `_winResize` (linha ~2222) ganhou um **"pump"**: durante o arrasto bombeia `_scheduleSize()` por rAF até `mouseup`/`blur` (cobre o loop modal que pode não disparar eventos a tempo). Cleanup no `componentWillUnmount`. Usa `this._root` (callback ref, linha ~4163) com fallback a `querySelector`.
  - **Verificação (puppeteer/Chrome, server isolado):** em 1280/980/1120/940px o root passa a seguir o viewport medido (`style.width/height` exatos, `--vw/--vh` ok, rect renderizado bate, sem overflow-x), zero erros novos (só os 404 de placeholder do DC). **RESSALVA:** o browser NÃO reproduz o bug do WebView2 (lá o viewport nunca fica stale), então isso confirma "código correto + mecanismo no lugar", não "resolvido no .exe" — validação real só no .exe do usuário após deploy.
  - **Trilha B (Rust, PRECISA rebuild) — preparada, NÃO aplicada ainda:** se a Trilha A não bastar, adicionar em `src-tauri/src/lib.rs` um `WindowEvent::Resized(size) => { if let Some(wv)=window.get_webview_window(window.label()) { let _=wv.set_size(*size); } }` no `on_window_event` (força o controller do WebView2 a recomputar Bounds). Risco: `set_size` durante o drag pode dar micro-jitter (tauri#10053) — aplicar só se necessário. Usuário JÁ autorizou o rebuild ("pode rebuildar") caso precise. Plano C (último recurso): `decorations(false).transparent(true)` + workaround tao#72.
- **2026-06-22 (cont.) — "dá pra abrir vários .exe ao mesmo tempo" = build ANTIGO instalado (não era bug de código). Rebuild do instalador 0.2.0→0.4.1.** Usuário reinstalou e notou que abria múltiplas instâncias. **Diagnóstico empírico:** o app instalado em `%LOCALAPPDATA%\Centro de Comando\centro-de-comando.exe` era **versão 0.1.0** (`VersionInfo`), que é ANTERIOR ao single-instance (adicionado só na 0.2.0, commits 4c7278b/de72277). Confirmado ao vivo: **3 processos** `centro-de-comando` rodando ao mesmo tempo. O código atual JÁ tem o single-instance correto (`tauri-plugin-single-instance` v2.4.2, registrado como 1º plugin no `lib.rs`, callback `show_main`); o usuário/amigos só tinham o instalador 0.1.0 (provavelmente o originalmente compartilhado). **Ação:** bump da versão do instalador (`Cargo.toml` + `tauri.conf.json`) **0.2.0→0.4.1** (alinha com o `_appVersion` web visível) e **rebuild release** (`npm run tauri build`, 3m53s) → `src-tauri/target/release/bundle/nsis/Centro de Comando_0.4.1_x64-setup.exe` (cópia no Desktop do usuário pra facilitar o envio). **Verificação empírica do single-instance no binário 0.4.1** (script Node: matar tudo → abrir 3× com 5s entre cada → contar processos): baseline 0, após 1ª/2ª/3ª = **1/1/1** (`singleInstanceOK:true`), 0 após limpeza — a 2ª/3ª abertura foca a janela aberta. **Pendência do usuário:** desinstalar o 0.1.0, instalar o `..._0.4.1_x64-setup.exe` e **reenviar esse instalador pros 2 amigos** (correção é NATIVA, não chega pelo deploy web como as outras). NOTA sobre versões: o `.exe` é só uma casca que carrega a URL hospedada — fixes WEB chegam sozinhos no deploy (não precisa reinstalar); só fixes NATIVOS (como single-instance) exigem reinstalar/redistribuir o instalador.
  - **RESOLUÇÃO (mesmo dia):** usuário mandou print mostrando 3 janelas abertas e que "ainda abre vários" + "dá pra ter vários instalados". Diagnóstico no registro: o instalado **continuava 0.1.0** (a 0.4.1 nunca tinha instalado — o app rodando trava os arquivos e impede o upgrade). E havia **só UMA** entrada no registro (o "vários instalados" era a confusão das múltiplas janelas; a config NSIS é `currentUser` = atualiza no mesmo lugar, não duplica). **Fiz a reinstalação limpa na máquina do usuário:** `taskkill` das 3 instâncias → instalar a 0.4.1 silenciosa (`Start-Process ... /S -Wait`, sem `-ExecutionPolicy Bypass` — esse foi bloqueado pelo classificador). Resultado no registro: **uma única** entrada `Centro de Comando 0.4.1`, mesma pasta — **upgrade in-place, sem duplicar**. **Verificado single-instance no app INSTALADO** (abrir 3× → 1 processo, `singleInstanceOK:true`). **Aprendizado p/ os amigos:** precisa FECHAR o app de vez (inclusive a bandeja / Task Manager → encerrar `centro-de-comando.exe`) ANTES de rodar o instalador, senão o upgrade falha silenciosamente e fica na versão antiga. Pendência: usuário reenviar o `..._0.4.1_x64-setup.exe` pros 2 amigos (que estão na 0.1.0).
- **2026-06-23 — Mobile: hero recolhível na aba Demandas (web, deploy). Versão web 0.4.1→0.4.2.** Usuário pediu (escolheu a opção "some tudo ao rolar, volta ao subir"): no celular, ao rolar a lista pra baixo o cabeçalho (título "Demandas" + subtítulo + a linha de controles escopo/visualização/filtro/Nova demanda) recolhe; ao rolar pra cima / chegar no topo, expande. **Implementação (mobile-only):** estado `demandHeroCollapsed`; listener de `scroll` em **FASE DE CAPTURA** no `document` (`this._demandScroll`, registrado no `componentDidMount`, removido no unmount) — pega o scroll de qualquer container interno já que cada view (Por cliente / Por status) tem seu próprio scroller; direção via `_demandScrollLast` com histerese de 6px e reset ao chegar no topo (≤4px). Guard `isMobile && screen==='demandas'`. CSS dentro do `@media(max-width:760px)`: `.cc-demand-hero { overflow:hidden; max-height:360px; transition... }` e `.cc-demand-hero.cc-collapsed { max-height:0; padding 0; opacity:0; translateY(-14px) }`. Hero (1º filho do `cc-screen-flex`, linha ~499) ganhou `class="{{ heroClass }}"` (binding = `'cc-demand-hero cc-collapsed'` quando mobile+colapsado, senão `'cc-demand-hero'`). `go()` reseta `demandHeroCollapsed:false` + `_demandScrollLast=0` ao trocar de tela. **Desktop intacto** (CSS é mobile-only + JS com guard `isMobile`). As abas de cliente e a barra do cliente ativo NÃO fazem parte do hero (continuam visíveis). **Verificado (puppeteer 390px, mobile/touch, 16 demandas):** hero inicial expandido (214px) → rolar scroller (scrollHeight 2388) → recolhido (height 0, classe `cc-collapsed`) → voltar ao topo → expandido (214px); 3 critérios OK, só os 404 de placeholder. Screenshot do recolhido conferido (lista ocupa a tela toda). NÃO precisa rebuild do .exe (carrega a web).
- **2026-06-23 (cont.) — Hero recolhível: CORRIGIDO oscilação + trava (reportados pelo usuário).** A 1ª versão (direção + histerese 6px) **oscilava** ("subindo e descendo e bugando") e **travava** ("não consigo mexer pra cima e pra baixo"). Causa: (1) recolher muda a altura do scroller → re-dispara scroll → loop de toggle; (2) em lista curta, recolher faz o conteúdo caber → sem rolagem pra voltar e expandir → hero some e não volta. **Fix (reescrita do `_demandScroll`):** trocado direção por **LIMIAR com banda de histerese larga** (recolhe quando `scrollTop>80`, expande quando `<28`), **COOLDOWN de 420ms** após cada toggle (ignora os scrolls disparados pelo próprio relayout/transição → quebra o loop), **GUARD de elegibilidade** (só recolhe se `scrollHeight - clientHeight - heroH > 48`, i.e., a lista é longa o bastante pra continuar rolável depois — lista curta NUNCA recolhe → sem trava), throttle por `requestAnimationFrame`, e altura do hero medida ao vivo (`offsetHeight`). `_setHero(v)` centraliza o toggle+cooldown; `go()` limpa o cooldown; unmount limpa timer/raf. **Trade-off consciente:** o hero agora volta ao **chegar no topo** (não em qualquer micro-rolagem pra cima) — escolha por robustez (qualquer "volta na subida" reabre a porta pra oscilação). Texto das Novidades 0.4.2 ajustado pra "reaparece ao voltar pro topo". **Verificado (puppeteer 390px):** lista longa (20) recolhe ao rolar + **8 scrolls espúrios não fazem flip** (não oscila) + volta no topo; lista curta (overflow 25px) **não recolhe** (não trava); 4/4 critérios, zero erros. Versão web segue 0.4.2 (bugfix da mesma feature).
- **2026-06-23 (cont.) — #17 RESOLVIDO no nativo (Trilha B): .exe não reflui em modo janela → fix em Rust + rebuild 0.4.1→0.4.2.** O usuário mandou 3 prints novos (em `screenshots/`, `Captura de tela 2026-06-23 0347*` e `035204`) confirmando que a **Trilha A (só web, deploy) NÃO resolveu**: no .exe em modo janela (não-maximizado), o conteúdo não encolhe junto com a janela e sobram **faixas pretas "mortas"** + elementos flutuando fora do lugar (player de música/X). As faixas pretas são a prova de que o problema é **NATIVO** (a área de desenho do WebView2 fica *stale*), não de layout DOM — por isso nenhum truque de JS resolve. Pesquisa web confirmou: é o bug **tauri-apps/tauri#6609** (`decorations:false` não reflui o conteúdo no resize), marcado **"won't fix"**; o único contorno citado lá é justamente JS (= Trilha A, que falhou aqui).
  - **Fix (Trilha B, `src-tauri/src/lib.rs`):** no `on_window_event`, novo braço `WindowEvent::Resized(size)` que re-aplica o tamanho **DO WEBVIEW** (itera `window.webviews()` → `wv.set_size(*size)`) — força o controller do WebView2 a recalcular os bounds. **NÃO causa loop:** redimensionar o webview (filho) não re-emite `WindowEvent::Resized` (que é só da janela). O `on_window_event` virou um `match` (CloseRequested + Resized + `_`). **Pegadinha de API (Tauri 2.11.3):** `Window::webviews()` retorna `Vec<Webview>` (não HashMap) → iterar, não `.get("main")`.
  - **Versão:** `Cargo.toml` + `tauri.conf.json` 0.4.1→**0.4.2** (alinha com a web e ajuda o NSIS a reconhecer como upgrade).
  - **Verificação:** `cargo check` OK; `npm run tauri build` (2m22s) gerou `Centro de Comando_0.4.2_x64-setup.exe` (instalador **~1,4 MB** — bem menor que os 26 MB da 0.1.0 porque NÃO embute mais `node.exe`/servidor; carrega a URL hospedada; binário 4,24 MB, VersionInfo 0.4.2 confirmada). Cópia no Desktop do usuário. **NÃO dá pra testar o resize do .exe daqui** (o bug não reproduz no browser e o loop modal de resize não é dirigível headless de forma confiável) — é fix de **melhor-esforço com o mecanismo nativo correto**; **a validação real depende do usuário** instalar e arrastar a borda da janela. Se ainda não refluir, Plano C (último recurso): `set_size` da JANELA no Resized, ou `decorations(false)+transparent(true)`.
  - **Pendência do usuário:** **FECHAR o app de vez** (janela + bandeja + Task Manager → encerrar `centro-de-comando.exe`) ANTES de instalar (app rodando trava os arquivos e o upgrade falha silencioso → fica na versão antiga), instalar o `..._0.4.2_x64-setup.exe`, testar o resize. Como é fix **NATIVO**, NÃO chega pelo deploy web — precisa reinstalar (e reenviar pros amigos se quiser que eles também peguem).
- **2026-06-23 (cont.) — Player de música "respeita a janela" (fix WEB, deploy). Versão web 0.4.3.** Depois que o resize do .exe foi corrigido (entrada acima — usuário confirmou "perfeito, arrumou"), surgiu o efeito colateral: o player de música ficava **cortado pra fora** da janela (print `Captura de tela 2026-06-23 045640.png`). Causa: o `_musicClampPos` (que re-encaixa o painel no viewport) só rodava ao **abrir** e ao **desminimizar** — não no **resize** da janela; então ao diminuir a janela o painel ficava preso na posição antiga, fora da tela. (O usuário também pediu "minimizador + playlist + criar/salvar" — mas tudo isso **já existia** no player; ele só não conseguia ver/usar porque estava cortado.)
  - **Fix (`Centro de Comando.dc.html`, 4 mudanças):** (1) re-encaixe no resize — `_applyRealSize` (que já roda a cada resize via ResizeObserver/`resize`/`onResized`) agora re-clampa o `musicPos` se o player estiver aberto (só faz `setState` se a posição mudou, sem loop). (2) `_musicClampPos` passou a **medir a altura REAL do painel** (`.cc-music-panel` → `offsetHeight`) em vez de chutar 372px fixo — encaixa certo nos estados minimizado (~46px)/aberto/com picker. (3) painel ganhou `max-height:calc(var(--vh,100vh) - 16px)` + `display:flex;flex-direction:column` e o corpo virou `overflow-y:auto;flex:1 1 auto;min-height:0` → nunca estoura a altura da janela, **rola por dentro** (usa o `--vh` medido da Trilha A, robusto mesmo se o `100vh` do webview ficar stale). (4) `toggleMusicPicker` re-clampa ~70ms após abrir (o painel cresce com o picker). Classe `cc-music-panel` adicionada ao painel pra medição.
  - **IMPORTANTE — chega no .exe pelo DEPLOY (não precisa reinstalar):** é fix WEB e o .exe carrega a URL hospedada → `git push` → Render redeploya → o player se conserta no .exe 0.4.2 já instalado. (Diferente do fix de resize, que era nativo.)
  - **Detalhe do breakpoint:** a música é **desktop-only** (≤760px = mobile, player escondido). O .exe tem `min_inner_size` 940px, então sempre é desktop — sem problema. (No teste, encolher pra 760 fazia o player sumir = comportamento correto, não bug.)
  - **Verificação (puppeteer-core/Edge headless, server isolado porta 5199, banco separado, login `musictest`):** **PASSOU** — abre encostado à direita em 1400×900 (visível); encolher pra **980×620** (pequeno mas ainda desktop) re-encaixa de `left:1100`→`left:692` (dentro da janela ✓); abrir o picker em 980×620 → painel limitado a 604px de altura, **rola por dentro** (`bodyScrolls:true`), totalmente visível ✓; minimizar → painel 46px visível ✓; minimizar/playlist/categorias/adicionar-link todos presentes ✓. Zero erros de console (fora os 404 de placeholder do DC). Screenshot conferido. `node --check` OK. Server isolado, temp e `puppeteer-core` (`--no-save`) removidos ao final.
- **2026-06-24 — #17 de novo: resize do .exe VOLTOU a falhar (Trilha B/`set_size` é instável). Trocado pela janela TRANSPARENTE. Instalador 0.4.3.** O usuário reportou ("o mesmo problema voltou") com print: na v0.4.2 (que tem a Trilha B `set_size` no `WindowEvent::Resized`), o conteúdo voltou a não refluir ao redimensionar — e **continua bugado até maximizado e em outras telas** (confirmado por pergunta dirigida, descartando o overflow normal do kanban). Confirmado também que o .exe instalado ERA mesmo o 0.4.2 com a Trilha B (VersionInfo + data de build batem) — ou seja, **a Trilha B não é confiável** pra esse bug (funcionou e depois voltou = intermitente).
  - **Diagnóstico (pesquisa web):** o `set_size` manual do webview é justamente um dos caminhos problemáticos (tauri#10053 "Manual WebView resizing does not work correctly" → posiciona errado; tauri#11975 "setSize não funciona com decoration:false"). O contorno **documentado** que funciona pro bug do WebView2 em janela sem bordas (tauri#6609/#9053) é deixar a **janela transparente** com o **fundo opaco do app por cima** — aí o WebView2 passa a acompanhar o redimensionamento de forma confiável.
  - **Fix (`src-tauri/src/lib.rs`):** adicionado `.transparent(true)` no `WebviewWindowBuilder` (junto do `.decorations(false)`). **Removido** o handler de `WindowEvent::Resized` com `set_size` (instável + podia desligar o auto-resize nativo que a transparência conserta) — o `on_window_event` voltou a tratar só o `CloseRequested`. Seguro visualmente: `body { background: var(--app-bg) }` é cor sólida (`#101114` dark / `#e9e3d9` light), cobre a janela toda → a transparência fica "tampada" pelo conteúdo opaco, visual idêntico. Versão nativa `Cargo.toml`+`tauri.conf.json` 0.4.2→**0.4.3** (alinha com a web e ajuda o NSIS no upgrade).
  - **Verificação:** `cargo check` OK; `npm run tauri build` (2m15s) → `Centro de Comando_0.4.3_x64-setup.exe` (~1,38 MB), binário com VersionInfo 0.4.3. Cópia no Desktop do usuário (o 0.4.2 antigo de lá foi removido). **NÃO dá pra testar o resize/transparência daqui** (o bug só reproduz no .exe real; o browser não reproduz; e o app estava RODANDO — PID em uso — então não lancei o build novo pra não brigar com o single-instance) → é o **contorno documentado**, mas a validação real depende do usuário instalar e testar. Se a transparência ainda não resolver (ou causar artefato visual tipo ver o desktop atrás), próximos: garantir `html` opaco também, ou Plano D (decorations nativas + barra custom como overlay).
  - **Pendência do usuário:** **FECHAR o app de vez** (estava rodando — bandeja → Sair, ou Task Manager → encerrar `centro-de-comando.exe`) ANTES de instalar o `..._0.4.3_x64-setup.exe`, senão o upgrade falha silencioso e fica no 0.4.2. Reenviar pros amigos (correção é NATIVA, não chega pelo deploy web).
  - **RESULTADO (confirmado pelo usuário via vídeo, 2026-06-24):** a janela transparente **RESOLVEU o reflow** — gravação de tela (28s, `screenshots/Gravação de Tela 2026-06-24 124150.mp4`) mostra o .exe 0.4.3 redimensionando por todas as telas (Demandas/Calendário/Lembretes/Clientes/Mapa mental/Financeiro/Metas) e o conteúdo acompanhando, **sem faixas pretas**. #17 (reflow) = resolvido no nativo. (Restou um efeito colateral só de overflow horizontal — ver entrada 0.4.4 abaixo.)
- **2026-06-24 (cont.) — Web 0.4.4: Demandas e Mapa mental "passavam pra direita" ao redimensionar o .exe (overflow horizontal).** No mesmo vídeo, o usuário apontou que nas abas **Demandas** e **Mapa mental (Workflow)** o conteúdo vazava pra fora da borda direita ao redimensionar (a coluna "Entregue" do kanban e os controles de zoom do mapa saíam da tela). Bug **transitório** (some quando para o arrasto → por isso os quadros parados do vídeo pareciam OK).
  - **Causa raiz:** as proteções `overflow-x:hidden`/`max-width` da raiz estavam **só dentro do `@media(max-width:760px)`** (mobile) — no desktop/.exe não havia teto. Some-se a isso o `_applyRealSize` (Trilha A), que **fixa `width` em px** na raiz a partir de `visualViewport.width`; no WebView2 transparente essa medida pode vir **MAIOR que a janela** durante/depois do resize. Aí a raiz fica mais larga que a janela e as duas telas **full-bleed** (`cc-screen-flex` SEM `max-width`: Demandas linha ~501 e Mapa linha ~1300) esticam o conteúdo até a borda da raiz → a parte direita (kanban tem `min-width:max-content`; o canvas tem controles ancorados em `right:16px`) é empurrada pra fora. Telas com `max-width` centralizado (Financeiro etc.) não mostram porque o conteúdo centraliza.
  - **Fix (`Centro de Comando.dc.html`, 2 camadas — à prova por construção):** (1) **CSS** — `[data-theme-root]` (linha 147) ganhou `max-width:100%; max-height:100%; overflow:hidden`. Como a raiz é `position:fixed`, `max-width:100%` resolve contra o viewport (containing block) → a raiz **nunca** excede a janela, não importa o px que a JS setar; `overflow:hidden` contém qualquer sobra transitória **sem cortar overlays** (modais/toasts/drawers são `position:fixed`, escapam do clip — verificado). (2) **JS** — `_applyRealSize` agora clampa `w/h` com `Math.min(medida, de.clientWidth/clientHeight)` (clientWidth do `<html>` = viewport de layout, não infla com filho overflow → teto seguro). As duas se reforçam. Só mexe em teto: **não regride o #17** (a raiz só pode ficar ≤ janela, que é sempre o correto pra um shell `inset:0`).
  - **Verificação:** `node --check` no bloco `data-dc-script` OK. **Teste de mecanismo em Chrome headless** (repro standalone com a mesma estrutura: raiz `position:fixed` forçada a `innerWidth+300`, sidebar+main `min-width:0`, kanban `overflow-x:auto` com `min-width:max-content`, controle ancorado em `right:16px`, modal `position:fixed`): **SEM fix** → `zoom.right=1668 > vw=1384` (vaza ✓ reproduz); **COM fix** (max-width+overflow+clamp) → `zoom.right=1368 ≤ 1384` (dentro ✓); **modal `position:fixed` visível e não cortado** nos dois (overflow:hidden não afeta overlays ✓). O bug do WebView2 em si não reproduz no navegador (é transiente nativo) → validação real do .exe depende do usuário, mas o teto agora é **garantia dura**. É fix **WEB** → chega no .exe 0.4.3 já instalado pelo **deploy do Render** (NÃO precisa reinstalar). Versão web `_appVersion` 0.4.3→**0.4.4** + entrada na tela de Novidades. (Commit nativo 0.4.3 `b58bd20`, que estava local-only, vai junto no push — não muda nada no app no ar, só sincroniza o GitHub.)
- **2026-06-24 (cont.) — Web 0.4.5: Mapa mental (Cérebro) ganhou GRAPH VIEW estilo Obsidian (Fase 1 do pedido do usuário, aprovada).** Baseado num 2º vídeo do usuário mostrando o Obsidian (graph view + canvas). Esta entrega = a fatia "Graph estilo Obsidian" (ele escolheu começar por ela; alvo do MCP quando for: Claude Desktop). Antes de implementar, rodei um workflow de auditoria (2 agentes) que confirmou: o Cérebro já tinha força-directed (`_wfForce`), cor por tipo/status, legenda-filtro, foco local + painel de conexões, pan/zoom/pinch e persistência — faltava o painel de controles, fade de label e busca.
  - **Novidades (`Centro de Comando.dc.html`):** (1) **Painel "Controles"** flutuante no topo-esquerdo do canvas (botão com ícone `filter`, `cc-hide-mob` = desktop-only, respeitando a memória "mobile é mais enxuto"; posicionado à esquerda pra não colidir com foco/zoom topo-direito nem legenda baixo-esquerda). 3 seções: **Filtros** (busca textual + pills Clientes/Demandas/Ideias), **Forças** (4 sliders: Repulsão/Distância das ligações/Força das ligações/Gravidade central), **Tela** (toggles Esmaecer rótulos / Tamanho por conexões). (2) **`_wfForce` parametrizado** — lê `wfRepel/wfLinkDist/wfLinkStr/wfGravity` do state (antes hardcoded 11000/120/0.05/0.006); mexer no slider re-roda a física com **debounce 130ms** (`_wfForceDebounced`) → sensação "ao vivo". (3) **Busca** acende o nó que casa e escurece o resto (entra na mesma computação de opacidade do foco/filtro/legenda). (4) **Fade de rótulo no zoom** (`wfLabelOpacity` derivado de `wfZoom`, curva (z-0.5)/0.32; **desligado no mobile** pra não esconder label em tela pequena). (5) **Tamanho do nó por grau real** (`wfDeg` a partir de `wfEdges`; multiplicador suave, máx +70%). (6) **Toggles de tipo** dimam por categoria via `wfKind(id)`. Parâmetros + toggles persistidos no `localStorage cdc-wf` (`_wfSaveNow`/`_loadWfView`).
  - **Verificação (puppeteer-core/Chrome headless, server isolado porta 5210 em `CDC_DATA_DIR` temp + Turso explicitamente VAZIO — zero risco ao banco de produção; signup-ou-login, seed de 2 clientes/4 demandas/2 ideias):** `node --check` OK; **11 nós** renderizam; painel completo (busca + **4 sliders** + 3 pills de tipo + 2 toggles de Tela); **busca "Editar"** → 10/11 escurecidos, 1 aceso ✓; **slider Repulsão** muda o layout (re-roda a física) ✓; **toggle "Demandas" off** → 5 nós escurecidos ✓; **fade no zoom** → opacidade do rótulo 1 → 0 ao afastar ✓; zero erros de console (fora o 409 benigno do signup de usuário já existente). Screenshots conferidos (painel + busca acendendo só "Editar vídeo de live"). Artefatos de teste, server isolado, data dir temp e `puppeteer-core` (`--no-save`) removidos ao final. É fix **WEB** → chega no .exe 0.4.4 pelo **deploy do Render** (sem reinstalar). `_appVersion` 0.4.4→**0.4.5** + Novidades. **Próximas fatias do pedido (aguardando OK):** Canvas com cards reais (embutir cliente/demanda/ideia como card vivo no Blueprint), depois MCP (Claude Desktop) e "links reais entre entidades".
