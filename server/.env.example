# Copie pra server/.env (nunca comitar o .env real).
# Gerado automaticamente por "npm run setup" — não escreva isto à mão.
APP_PASSWORD_HASH=

# Usado a partir da Fase 3b (assistente IA real). Fica só aqui, nunca no navegador.
GEMINI_API_KEY=
